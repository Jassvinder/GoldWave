# System Architecture

## Status

- Status: Active
- Last verified: 15-09-2026 — T-020 (automated test coverage, concurrency focus): a new `tests/Concurrency/` PHPUnit testsuite (own config, `phpunit.concurrency.xml`, never merged into the default `phpunit.xml` since it must run against the real dev Postgres database, not SQLite `:memory:` — SQLite can't produce a genuine row-level-lock race and can't even be shared across the separate OS processes these tests launch) proves 3 financial critical sections stay correct under real parallel access, not just sequential retries: Store Wallet deduction (`StoreWalletService::deduct()`), Store Inventory decrement (`ConfirmStoreSale`), and the Booster Payout Processor (`ProcessBoosterPayouts`) — each backed by a tiny, single-purpose `concurrency:*` artisan command (`app/Console/Commands/Concurrency*.php`, test-worker-only, never user-facing) launched several times simultaneously via `Illuminate\Support\Facades\Process::start()`, asserting the database's exact final state (no lost update, no overdraft, no double-payment). Run explicitly: `php artisan test` with the `configuration` option set to `phpunit.concurrency.xml` — excluded from the default `php artisan test` run, which stays 100% SQLite-based. `tests/Pest.php` gained a second `pest()->extend(TestCase::class)->in('Concurrency')` binding (deliberately no `RefreshDatabase`, which would wipe real dev data — each test creates and cleans up its own throwaway fixtures instead). Building these tests surfaced one real, separate schema gap (not a concurrency-safety bug — the locking itself is correct): `booster_payout_schedules.wallet_ledger_entry_id` had no `ON DELETE` action, which could block deleting a Member with paid booster history; fixed with a new migration setting that FK to `ON DELETE SET NULL`. Also audited `Docs/TEST.md`'s full Risk-based Coverage table row-by-row and filled 2 real, narrow gaps found in already-existing test files (a Sponsor-vs-Binary-Position divergence proof for Level Income, a symmetric Right-side occupied-traversal test for binary placement — see `DOMAIN_LOGIC.md` §21's T-020 build & QA entry for the full detail on every row's audit result).
- Last verified: 14-09-2026 — T-018 (Reports module): a new, separate, Super-Admin-only Reports Center (`SuperAdmin\ReportsController`, `super-admin/reports`) covering all 11 `INSTRUCTIONS.md` "Reports" table rows — does not touch M17 (Member's own small/synchronous reports, T-015) or A06 (Admin's own store-scoped reports, T-016). Every export request is queued uniformly (never generated synchronously on the request thread, per `PERFORMANCE_GUIDE.md`) through a new `report_exports` tracking table (`pending`→`processing`→`ready`/`failed`), a new `Actions/Reports/RequestReportExport`, and a new `Jobs/ProcessReportExport implements ShouldQueue`. A new `App\Services\ReportCatalog` holds one method per report type, each a `cursor()`-backed `LazyCollection` query (never `get()->all()` — these tables can hold tens of thousands of rows) against the already-existing models (`Member`, `EmiInstallment`, `IncomeLedgerCalculation`, `PairRewardTransaction`, `DrawExecution`, `BoosterPayoutSchedule`, `Payment`, `PayoutTransaction`, `WalletLedgerEntry`, `StoreSale`, `StoreProfitDistribution`) — no new report-data tables, only the tracking table. 2 new composer packages: `maatwebsite/excel` (`.xlsx`, via a new `App\Exports\ReportExportSheet`) and `barryvdh/laravel-dompdf` (`.pdf`, via a new `resources/views/reports/export-pdf.blade.php`); CSV is written directly with `fputcsv()` against a stream, properly escaping commas/quotes (unlike M17/A06's older, naive `implode(',', ...)` CSV pattern, left as-is since neither of those pages' columns actually contain a comma-risk field today). New `App\Support\Dates::display()` — DD-MM-YYYY formatting for dates written directly into a server-generated file, where no frontend `formatDate()` pass ever runs. New `resources/js/pages/super-admin/reports.tsx` + a new "Reports" `super-admin-sidebar.tsx` section. **A genuinely queued job needs a real queue worker running continuously in production** (`php artisan queue:work`, typically under Supervisor) — `QUEUE_CONNECTION=database` was already configured (unused until this task); installing/configuring that persistent worker process is a deployment decision, explicitly deferred the same way T-010 deferred choosing a real broadcast driver — not a T-018 blocker, since the Job itself is code-complete and was manually verified end-to-end via `php artisan queue:work --stop-when-empty` against the real dev DB (3 real exports requested through the browser — csv/xlsx/pdf — all processed correctly, downloaded, and their file contents verified byte-for-byte correct, including a comma-containing field round-tripping correctly through CSV).
- Last verified: 14-09-2026 — T-017 (Super Admin Portal): 13 new `Http/Controllers/SuperAdmin/*` controllers covering S01–S10 plus Admin Dashboard/Member Management/Compensation Management/Draw Management (`DashboardController`, `AdminUserController`, `RuleVersionController`, `DummyEntrySettingsController`, `DummyEntryAssignmentController`, `DrawSettingsController`, `DrawManagementController`, `MetalRateController`, `PayoutTdsSettingsController`, `StoreManagementController`, `StoreWalletManagementController`, `MemberManagementController`, `CompensationManagementController`), joining the existing `CashPaymentApprovalController` (T-003) in the same namespace; the top-level `DashboardController` gained a `role === 'super_admin'` branch delegating to `SuperAdmin\DashboardController` (S01), mirroring M01/A01's precedent — the generic starter-kit placeholder is now dead code for every real role. 5 new Actions built from this file's own T-017 pre-coding pass (see `DOMAIN_LOGIC.md` §21): `Actions/Settings/PublishRuleVersion` (the one shared writer behind S03/S04/S06/S08/Admin-Compensation-config's versioned settings), `Actions/Settings/SetMetalRate` (S07), `Actions/Admin/CreateAdminUser` (S02), `Actions/Store/ReassignStoreOwner` (S09), `Actions/Draw/ReconcileDrawExecution` (S06's reconciliation half — 2 new `draw_executions` columns + a new `draw_execution_corrections` audit table). New `resources/js/layouts/super-admin-layout.tsx` + `super-admin-sidebar.tsx` for every `super-admin/*` page (6 grouped nav sections, 13 items). Full manual browser QA (all 16 screens, several real writes including a rule-version publish and a draw reconciliation) found and fixed 3 real issues — a Recent-Activity-feed sort-order bug, a stats double-count of the seeded company-root Member, and a raw-UUID display issue in the Store Wallet ledger — see `DOMAIN_LOGIC.md` §21's T-017 build & QA entry for the full detail.
- Last verified: 14-09-2026 — T-016 (Admin/Store Owner Portal): 5 new `Http/Controllers/Admin/*` controllers (A02-A06: `StoreProfileController`, `SalesController`, `InventoryController`, `StoreTransactionsController`, `StoreReportsController`) plus `DashboardController` gaining an admin-with-store branch that delegates to a new `Admin\StoreDashboardController` (A01, same shared `/dashboard` route M01 uses); new `EnsureStoreOwnership` middleware (alias `store-owner`) resolves the one `Store` a `role=admin` user owns and 403s if none; new `App\Services\StoreActivityLogger` — every T-014 Store Action now writes a `store_activity_logs` row through it, a DOMAIN_LOGIC.md §16.3 requirement that existed in the schema since T-014 but had never actually been implemented until this task's own QA surfaced the gap; new `resources/js/layouts/admin-layout.tsx` + `admin-sidebar.tsx` for every `admin/*` page. Also fixed two real bugs found during this task's required manual browser QA (not business-rule questions) — a Card-list item-row visual overlap on 3 pages, and the Indian DD-MM-YYYY date format never actually being applied anywhere in the running app despite being a hard requirement since T-003 (new `formatDate()` helper in `resources/js/lib/utils.ts`, applied at every dynamic-date display site across both the Member and Admin portals) — see `DOMAIN_LOGIC.md` §21's T-016 entry for the full detail.
- Last verified: 14-09-2026 — T-015 (Member Portal): 14 new `Http/Controllers/Member/*` controllers (M02-M05/M07/M10-M18) plus a top-level `DashboardController` (M01, shared `/dashboard` route), all calling straight through to Actions T-006-T-014 already built; `App\Support\Dates` helper for the recurring Larastan/Carbon date-cast issue; `resources/js/layouts/member-layout.tsx` + `member-sidebar.tsx` replacing the generic `AppLayout` for every `member/*` page. Also, 13-09-2026 — T-019 (Wire scheduled jobs): all 6 recurring §19 jobs registered via a new `App\Console\Scheduling::register()` class (called from `bootstrap/app.php`'s `withSchedule`), each with an explicit `Asia/Kolkata` timezone and `withoutOverlapping()`; 2 new Job wrappers (`RunDrawGroupGeneration`, `RunMonthlyDrawExecution`) for T-010's Draw Actions, which had none yet. Also, T-014 (Store module) built against this file's pre-coding pass notes: `Store/` Actions (`CreateStore`, `AllocateStoreInventoryItem`, `RecordStoreWalletTopup`, `ConfirmStoreSale`, `RecordItemBuyback`, `RecordPlanJewelleryDelivery`), `StoreWalletService`, `Compensation/{CalculatePurchaseRepurchaseIncome,CalculateStoreProfitDistribution}`, `Events/StoreSaleConfirmed` + 2 listeners, all as anticipated. Compensation Engine rules gained rule 6 (Buyback is deliberately outside the compensation-engine call chain) and a numbering fix (rule 5 had been duplicated since T-013).
- Owner / evidence: Decided before any GoldWave business-logic code was written (per `Docs/advice.md`'s pre-coding checklist), against the schema finalized in T-002 (`Docs/DATABASE_SCHEMA.md`) and the rules in `Docs/DOMAIN_LOGIC.md`.

This file is the canonical home for **how** the code is organized — folder/namespace structure, layering, where business logic lives, how the compensation engine stays a single source of truth, job/event wiring, and the permission structure. It does not restate business rules (`DOMAIN_LOGIC.md`), page contents (`INSTRUCTIONS.md`), or schema (`DATABASE_SCHEMA.md`) — it says where the code that implements them goes.

## Layering (matches AGENTS.md's MVC principles, made concrete)

```
HTTP request
  → Route (routes/web.php, grouped by role: member / admin / super-admin)
  → Middleware (auth, role-gate — see Permissions below)
  → Controller (thin: validates via a Form Request, calls one Action/Service, returns an Inertia response)
  → Form Request (validation only — every field, every rule; controllers never inline-validate)
  → Action or Service (the business logic — see Compensation Engine below)
  → Model / Eloquent (persistence only — relationships, casts, scopes; no calculation methods on Models)
  → Inertia response (React page component receives typed props; no calculation happens in React — DOMAIN_LOGIC.md's Architecture Principle)
```

Controllers must stay thin enough that reading one tells you _what_ happens, not _how_ — the "how" lives in a Service/Action class named after the business operation (e.g. `ConfirmEmiInstallmentPayment`, not logic inlined in `EmiController@pay`).

## Folder / Namespace Structure

```
app/
  Actions/                  One class per single business operation, invokable (__invoke), the unit T-level tasks are built around
    Registration/           RegisterMember, ValidateSponsorCode, CalculateEmiRateBooking, ActivateMembershipOnPaymentConfirmed (T-003); GenerateEmiInstallments (T-005 — called by ActivateMembershipOnPaymentConfirmed right after the existing emi_schedules header-row creation, generates all emi_installments rows per the activation-date-anniversary cadence, DOMAIN_LOGIC.md §5 item 7)
    Payments/               ConfirmOnlinePayment, ApproveCashPayment, RejectCashPayment (T-003); ConfirmEmiInstallmentPayment (T-005 — mirrors ConfirmOnlinePayment/ApproveCashPayment for a specific emi_installments row instead of the registration payment, dispatches the same PaymentConfirmed Event so future Level Income/Pair Entry listeners (T-006/T-007) need no EMI-specific wiring)
    Auth/                   RequestLoginOtp, VerifyLoginOtp, LoginWithCustomerIdPassword, RequestPasswordResetOtp, VerifyPasswordResetOtp, SetNewPassword (T-003 — not in the original plan below; added once the Login & Authentication mechanism was designed, DOMAIN_LOGIC.md §2.2)
    Compensation/           CalculateLevelIncome (T-006 — Sponsor/Direct levels 1-12, `skipped` rows for a too-short chain or an inactive beneficiary, DOMAIN_LOGIC.md §6), CreatePairEntries + EvaluatePairMilestones (T-007 — team-size fan-out to every Binary Position ancestor + monthly incremental milestone consumption, DOMAIN_LOGIC.md §7), EvaluateBoosterQualification (T-011 — registration-payment-only trigger, live binary-team-size recompute via BinaryTeamSizeCounter, independently-enforced Left/Right split, every newly-qualified level gets its own concurrent 6-month schedule, DOMAIN_LOGIC.md §9),
                             CalculatePurchaseRepurchaseIncome (T-014 — pays the purchasing member "self" 2% then walks their Sponsor/Direct chain 1-12, only when store_sales.member_id is set, DOMAIN_LOGIC.md §15), CalculateStoreProfitDistribution (T-014 — fires on every confirmed store sale regardless of member_id, owner 2% + the owner's own Sponsor/Direct chain levels 1-3, DOMAIN_LOGIC.md §16.4; both listen on StoreSaleConfirmed independently)
    Draw/                   GenerateDrawGroups (T-010 — forms as many full groups as the eligible/ungrouped backlog allows, ordered by members.id; never a partial group), ExecuteMonthlyDraw (per active group: idempotent per cycle month, requires a pre-configured draw_group_month_configs row or skips, secure RNG winner selection with an auditable rng_proof, upline benefit at >=10 Direct Members, fires DrawResultPublished)
    Payout/                 SubmitPayoutRequest (T-009 — validates min amount/available balance, places wallet hold at submission), ProcessPayoutRequest (creates immutable payout_transactions snapshot, confirms hold, bulk-capable via shared batch_reference), FailPayoutRequest (audit transaction row + releases hold, never confirms), RejectPayoutRequest, CancelPayoutRequest (both release the hold, zero transaction rows, only valid while status=pending)
    Store/                  CreateStore (T-014 — Super Admin opens a store, creating its Store Wallet and crediting the initial advance amount, §16.1), AllocateStoreInventoryItem (adds/increments a `store_inventory_items` row, §16.5), RecordStoreWalletTopup (thin wrapper over StoreWalletService::topUp), ConfirmStoreSale (validates/decrements `store_inventory_items` stock atomically with the sale per §16.8, blocked on insufficient stock; deducts the Store Wallet atomically when `paymentSource=store_wallet`; generates the Invoice; dispatches StoreSaleConfirmed after its own transaction commits, matching ApproveCashPayment's post-commit dispatch convention), RecordItemBuyback (§16.7/§16.9 — creates a `store_buybacks` row + adds stock back to `store_inventory_items`; never fires Store Profit Distribution/Purchase-Repurchase Income), RecordPlanJewelleryDelivery (§16.10 — marks a `product_benefits` row delivered through a store, then calls ConfirmStoreSale itself rather than duplicating sale-creation logic)
    Profile/                SubmitPendingProfileFields (T-012 — one-time, PAN/Aadhaar format-validated, Bank Account Details + Passbook/Cheque create the member's one member_bank_details row rather than separate members columns), VerifyMemberBankDetail (T-012 — Super Admin, closes a precondition T-009's SubmitPayoutRequest assumed but never built), SubmitProfileChangeRequest (guards one in-flight request per field), Approve/RejectProfileChangeRequest (approval updates the underlying field/bank-details row and resets its verified_at; both notify the member via Notifications/ProfileChangeRequestReviewed)
    DummyEntries/            GenerateDailyDummyEntries (T-013 — reads the enable/count settings, places each entry via occupied-side traversal off the seeded company root Member, real sequential Customer IDs), AssignDummyEntryToLeader (T-013 — same member row/id/Customer ID/tree position, creates the leader's first `users` row, no plan/payment required)
  Contracts/                Interfaces for vendor-deferred integrations (T-003) — every Action depends on the interface, never a concrete gateway/channel class
    PaymentGatewayContract.php  createIntent()/verifyCallback(); bound to Services/Payments/FakePaymentGateway.php until a real gateway is chosen
    OtpChannelContract.php      send(); bound per-channel to Services/Otp/EmailOtpChannel.php (real, via Mail) and SmsOtpChannel.php (logs — no SMS vendor chosen yet)
  Services/
    WalletLedgerService.php   The ONLY class allowed to write wallet_ledger_entries or mutate members.wallet_balance/wallet_hold_amount. credit() front-loaded in T-006 (needed by CalculateLevelIncome per Compensation Engine rule 3 below); T-008 completed it with hold()/confirmHold()/releaseHold() (DOMAIN_LOGIC.md §11.1's Wallet Hold rule — a pending debit ledger row whose own status transitions to confirmed/reversed rather than a second row being created, since §22's "reverse/correct through linked transactions" governs fixing a mistake, not a hold's own declared lifecycle) and availableBalance() (wallet_balance minus wallet_hold_amount); T-009 (Payout request/processing) calls these rather than building its own debit logic
    StoreWalletService.php    The ONLY class allowed to write store_wallet_ledger_entries or mutate store_wallets.balance (T-014) — advanceCredit()/topUp() (credit-side) and deduct() (checks sufficient balance first, blocks via ValidationException on insufficient funds, atomic with whatever record the caller creates in the same transaction, DOMAIN_LOGIC.md §16.1). No hold/confirm/release lifecycle like WalletLedgerService — DOMAIN_LOGIC.md never describes a Store Wallet reservation step, only an immediate balance check before a store-initiated payment confirms.
    RuleVersionService.php    Reads the active rule_versions/rule_values snapshot (caches only the scalar id, never the Eloquent model); every Compensation/pricing Action depends on this, never on a raw Settings table lookup (T-003)
    OtpService.php            The ONE place otp_codes rows get created/verified (T-003) — every Auth Action goes through this rather than querying otp_codes directly
    SponsorChainResolver.php  Walks members.sponsor_id up to N levels — the ONE place Level Income, Purchase/Repurchase Income, and Store Profit Distribution all resolve their beneficiary chain from (T-004; also gates Directs View navigation via isSelfOrDescendant — no compensation Action consumes the level-walk yet, that starts at T-006)
    BinaryPlacementResolver.php  Implements the occupied-side traversal (DOMAIN_LOGIC.md §4.3) — the ONE place placement_parent_id gets decided (T-003; gained an equivalent isSelfOrDescendant check in T-004 for Tree View navigation; gained ancestorsWithSide() in T-007 — the unbounded Binary Position ancestor walk CreatePairEntries fans out to, DOMAIN_LOGIC.md §7; EvaluateBoosterQualification (T-011) calls it with an explicit 10,000-level depth override, well past the 500-level default trip-wire, since occupied-side traversal can legitimately produce very deep chains for a large team)
    BinaryTeamSizeCounter.php  T-011 — a single recursive SQL CTE per ancestor returning both Binary Position legs' live descendant counts, for Income Booster's Team-size (with Left/Right split) thresholds (DOMAIN_LOGIC.md §9); a live recompute, unlike Pair/Reward's (§7) permanent consumable-entry ledger — chosen over per-node PHP recursion since Level 3's threshold alone is a 3,000-member team
    CustomerIdGenerator.php   Allocates sequential GWL0N… Customer IDs from a single locked counter row (T-003)
    Payments/FakePaymentGateway.php, Otp/EmailOtpChannel.php, Otp/SmsOtpChannel.php  Dev/default bindings for the Contracts above (T-003)
  Models/                   Eloquent models — relationships and casts only, no business methods
  Http/
    Controllers/
      Registration/         RegistrationController (public: show/validateSponsor/store/status) (T-003)
      Auth/                 GoldWaveLoginController, GoldWavePasswordResetController — Member-only login, distinct from the untouched Fortify-scaffolded generic /login (left in place as a plausible future Admin/Super Admin login path — the source spec never defines one) (T-003)
      Payments/             PaymentWebhookController (CSRF-exempt), PaymentDevSimulationController (non-production only) (T-003)
      SuperAdmin/           CashPaymentApprovalController (T-003, cash-registration approval only — store-scoped Admin cash approval deferred to T-014's store-initiated payment flow); DashboardController (S01), AdminUserController (S02), RuleVersionController (S03, also backs Admin Compensation Management's config page), DummyEntrySettingsController (S04, plus a manual `generateNow()` trigger over the existing `GenerateDailyDummyEntries` Action), DummyEntryAssignmentController (S05, thin wrapper over T-013's existing `AssignDummyEntryToLeader`), DrawSettingsController (S06 settings half) + DrawManagementController (S06 reconciliation half + Admin Draw Management), MetalRateController (S07), PayoutTdsSettingsController (S08), StoreManagementController (S09, create/reassign-owner/status — reuses T-014's `CreateStore`), StoreWalletManagementController (S10, reuses T-014's `RecordStoreWalletTopup`), MemberManagementController (Admin Member Management — list/detail/CSV export), CompensationManagementController (Admin Compensation Management's audit page) — all T-017; ReportsController (T-018 — the full 11-report Reports Center, index/store/download, queued exports)
      Member/               DirectsController, TreeController (T-004 — both serve Member and Super Admin from one controller each, gated by MemberPolicy); EmiController (T-005, polished to the full M06 spec in T-015); ProfileController, PendingProfileController, ChangeRequestController, MembershipController, PaymentHistoryController, LevelIncomeController, PairRewardController, BoosterController, DrawController, WalletController, PayoutController, ReportsController, NotificationController (all T-015 — M02-M05/M07/M10-M18; each does `$member = $request->user()->member; abort_if($member === null, 404);` then renders its page, calling straight through to the same Actions T-006-T-014 already built rather than duplicating any business logic in the controller)
      Admin/                 StoreDashboardController (A01, delegated to from the shared `/dashboard` route the same way M01 is), StoreProfileController (A02, `contact`/`location` only editable — store creation/ownership/status/wallet funding stay Super-Admin-only per §16.1/§17.2), SalesController (A03 — `storeSale`/`storeBuyback`/`storeDelivery` reuse T-014's `ConfirmStoreSale`/`RecordItemBuyback`/`RecordPlanJewelleryDelivery` Actions untouched, "new joining payments" interpreted as `RecordPlanJewelleryDelivery`), InventoryController (A04, read-only — Admin cannot allocate inventory themselves, Super-Admin-only per §16.1), StoreTransactionsController (A05), StoreReportsController (A06, CSV exports mirroring Member Portal's ReportsController pattern) — all T-016
    Requests/                One Form Request per write endpoint, named after the action (e.g. SubmitPayoutRequestRequest); T-003 added Registration/ and Auth/ subfolders; T-015 added Profile/ (SubmitPendingProfileFieldsRequest, SubmitProfileChangeRequestRequest) and Payout/ (SubmitPayoutRequestRequest); T-016 added Admin/ (RecordStoreSaleRequest, RecordItemBuybackRequest, RecordPlanJewelleryDeliveryRequest, UpdateStoreProfileRequest)
    Middleware/              EnsureRole (member/admin/super_admin — implemented T-003), EnsureStoreOwnership (alias `store-owner`, T-016 — resolves the one `Store` a `role=admin` user owns via `Store::where('owner_user_id', $user->id)->first()`, 403s if none, attaches it to the request for controllers to read)
  Policies/                  One per model needing authorization (MemberPolicy, StorePolicy, PayoutRequestPolicy, ProfileChangeRequestPolicy, ...) — see Permissions below
  Jobs/                      Queued jobs — see Background Jobs below
  Console/                  Scheduling.php (T-019 — the actual `Schedule::job(...)` registrations for every §19 recurring job, called from `bootstrap/app.php`'s `withSchedule`; kept out of that closure so it's directly unit-testable against a plain `new Schedule()` instance, see Background Jobs below)
  Events/ + Listeners/       PaymentConfirmed (T-003) → CalculateLevelIncomeOnPaymentConfirmed (T-006) + CreatePairEntriesOnPaymentConfirmed (T-007) + EvaluateBoosterQualificationOnPaymentConfirmed (T-011 — the Action itself filters to `registration` payments only, since Booster's structural inputs never change on a later EMI installment), all auto-discovered — Laravel 11+ event discovery, no manual Event::listen wiring needed, dispatched independently off the same confirmed payment; DrawResultPublished (broadcast, T-010 — fired directly by `ExecuteMonthlyDraw`, not event-listener-wired since there's no upstream domain event to hang off), StoreSaleConfirmed (T-014) → CalculateStoreProfitDistributionOnStoreSaleConfirmed + CalculatePurchaseRepurchaseIncomeOnStoreSaleConfirmed, both auto-discovered and dispatched independently off the same confirmed sale by `ConfirmStoreSale`, after its own transaction commits (matching `ApproveCashPayment`'s post-commit dispatch convention) — the Purchase/Repurchase listener's Action is a no-op when the sale has no purchasing member
  Notifications/            ProfileChangeRequestReviewed (T-012 — database channel only, the first notification this project sends; Member gained the Notifiable trait for this); cash-payment status, payout status (not yet built)
```

**M01 (Dashboard), A01 (Store Dashboard), and S01 (System Dashboard) are all the shared `/dashboard` route** (`Http/Controllers/DashboardController`, top-level, not under `Member/`, `Admin/`, or `SuperAdmin/`) — every login redirects to `route('dashboard')`, so this one controller branches: `$request->user()->member` present → the real M01 page; else an admin-with-a-store (resolved the same way `EnsureStoreOwnership` does) → sets `$request->attributes->set('store', $store)` and delegates to `Admin\StoreDashboardController::index()` (T-016); else `role === 'super_admin'` → delegates to `SuperAdmin\DashboardController::index()` (T-017); anyone else (an admin with no assigned store) still gets the generic starter-kit placeholder unchanged — the untouched placeholder is now unreachable for every real, fully-onboarded role. Admin/Super Admin login stays the untouched generic Fortify `/login` (email+password) — the source spec never defines a dedicated Admin/Super Admin login mechanism, so there was nothing to build here beyond the dashboard branch.

`App\Support\Dates::date(mixed $value): ?string` (T-015) — every Member Portal and Admin Portal controller that formats a *custom-cast* date/datetime attribute (not `created_at`/`updated_at`, which Larastan already infers correctly) for an Inertia prop calls this instead of `$value?->toDateString()`, which Larastan 3.11 flags as "cannot call method on string" for the same reason `ARCHITECTURE.md`'s existing Larastan/Carbon tooling note describes — centralizes a workaround that would otherwise repeat at nearly every date field across a dozen controllers. It intentionally returns plain ISO `YYYY-MM-DD` — display formatting is a separate, frontend-only concern (see `formatDate()` below).

`resources/js/lib/utils.ts`'s `formatDate(value: string | null | undefined): string` (T-016) — the single shared helper that renders any backend ISO date/datetime string in the project's required Indian DD-MM-YYYY display format (AGENTS.md "Conventions", a hard requirement since T-003 that had never actually been wired up anywhere before this task's QA caught it — see `DOMAIN_LOGIC.md` §21). Applied at every dynamic-date display site across both `member/*` and `admin/*` pages; returns `'—'` for null/undefined and leaves a non-`YYYY-MM-DD`-prefixed string (e.g. a `calculated_for_month` `YYYY-MM` value) untouched. The backend continues to hand the frontend plain ISO strings via `Dates::date()` — DD-MM-YYYY is applied purely at render time, never stored.

`App\Services\StoreActivityLogger` (T-016) — the one place every T-014 Store Action (`CreateStore`, `AllocateStoreInventoryItem`, `RecordStoreWalletTopup`, `ConfirmStoreSale`, `RecordItemBuyback`) writes a `store_activity_logs` row after its own write, satisfying DOMAIN_LOGIC.md §16.3's activity-log requirement (present in the schema since T-014 but never actually implemented until this task added it). `RecordPlanJewelleryDelivery` needed no direct change since it already calls `ConfirmStoreSale` internally, which now logs automatically.

`resources/js/layouts/member-layout.tsx` + `resources/js/components/member-sidebar.tsx` (T-015) — every `member/*` Inertia page now resolves to this shell instead of the generic starter-kit `AppLayout` (`resources/js/app.tsx`'s layout switch gained a `name.startsWith('member/')` case). `MemberSidebar` groups all 18 M01-M18 nav items (Overview / Profile / Membership & Payments / My Network / Income / Draw & Wallet / More) and reuses the existing `NavUser`/`AppSidebarHeader`/`AppShell` primitives unchanged — only the sidebar's own nav-item list is Member-Portal-specific. M08/M09 (Directs/Tree View, T-004) render inside this shell now too, having previously sat inside the generic `AppLayout` with no real Member Portal navigation around them.

`resources/js/layouts/admin-layout.tsx` + `resources/js/components/admin-sidebar.tsx` (T-016) — mirrors `member-layout.tsx`/`member-sidebar.tsx`'s structure; every `admin/*` page resolves to this shell (`app.tsx`'s layout switch gained a `name.startsWith('admin/')` case). `AdminSidebar` groups the 6 A01-A06 nav items under one "Store Operations" section.

**Public/pre-auth Controllers are organized by flow, not by role** (`Registration/`, `Auth/`, `Payments/`) — the Member/Admin/SuperAdmin role-folder split below only applies once a role context exists (after login/activation). This is a deliberate, documented deviation from this file's original Controllers layout (written before the Login & Authentication mechanism existed), not drift.

**Naming rule:** every non-trivial business operation is a named Action class, not a method buried in a fat Service. A Service only exists when several Actions need to share one clearly-owned piece of state or logic (the four Services above: wallet, store wallet, rule versions, chain resolution) — this avoids both "fat controller" and "fat service" anti-patterns without introducing a needless extra layer for every single operation.

## Compensation Engine — Single Source of Truth

This is the highest-risk part of the codebase (`DOMAIN_LOGIC.md` §0's "Income Calculation Integrity"), so its isolation rule is explicit:

1. **Every compensation Action reads configuration through `RuleVersionService`**, never a raw query against `rule_values` — this is what makes "store the rule_version_id used" (`DOMAIN_LOGIC.md` §22) automatic rather than something each Action has to remember.
2. **Every compensation Action resolves its beneficiary chain through `SponsorChainResolver`** — this is the one place that guarantees Level Income, Purchase/Repurchase Income, and Store Profit Distribution all walk `sponsor_id`, never `placement_parent_id` (`DOMAIN_LOGIC.md`'s Global Level-Based Income Rule). A compensation Action must never touch `placement_parent_id` directly.
3. **Every compensation Action writes its result by calling `WalletLedgerService::credit(...)`**, never by writing to `wallet_ledger_entries` or `members.wallet_balance` directly. This is what makes "never update a wallet balance without an associated ledger transaction" (`DOMAIN_LOGIC.md` §12) structurally true instead of a convention someone can forget.
4. **Idempotency is enforced at the data layer, not just in application logic**: `payments.idempotency_key` / `payments.provider_reference` are unique constraints (already in the schema), `draw_group_month_configs`/`draw_executions` have unique constraints on `(draw_group_id, cycle_month_no)`, `booster_payout_schedules` on `(booster_qualification_id, month_no)`, `pair_reward_transactions` on `(member_id, milestone_no)`, `store_profit_distributions` on `(store_sale_id, beneficiary_type)`. A duplicate attempt hits a DB constraint violation, not just a service-layer check that could have a race condition.
5. **Any Action that walks the Binary Position ancestor chain (`BinaryPlacementResolver::ancestorsWithSide()`) must skip an ancestor where `is_company_dummy = true && dummy_status !== 'assigned'`** before treating it as a compensation beneficiary (`DOMAIN_LOGIC.md` §14.2 point 5) — a dummy/the seeded company root can correctly still occupy a real Binary Position slot for a _different_ real ancestor's structural counts, but must never itself receive a payout. `CreatePairEntries`/`EvaluateBoosterQualification` (T-013 fix) apply this; any future Action walking this same chain (none currently planned) must too.
6. **`Actions/Store/RecordItemBuyback` is deliberately outside this list** — a Buyback is the store paying a member to bring stock back in, not a compensation event, so it never calls `SponsorChainResolver`/`WalletLedgerService::credit()` and creates no `store_profit_distributions`/`income_ledger_calculations` rows (`DOMAIN_LOGIC.md` §16.9). It still snapshots `rule_version_id` (rule 1) since its buyback-percentage rate is itself a versioned setting.
7. Web, Admin, and any future React Native app all call the **same Actions** (directly from Controllers today; behind an API layer later) — this is what makes "must not be implemented independently in multiple controllers/pages/mobile screens" (`DOMAIN_LOGIC.md` Architecture Principle) true by construction rather than by discipline.

## Background Jobs & Events

| DOMAIN_LOGIC.md §19 job               | Laravel implementation                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Daily Company Direct Generator        | `Jobs/RunDailyDummyEntryGeneration` (T-013) wraps `Actions/DummyEntries/GenerateDailyDummyEntries`; scheduled daily at 00:30 Asia/Kolkata (T-019)                                                                                                                                                                                                                                                                |
| Draw Group Generator (15th, 00:00)    | `Jobs/RunDrawGroupGeneration` (T-019) wraps `Actions/Draw/GenerateDrawGroups`; scheduled `monthlyOn(15, '00:00')` Asia/Kolkata                                                                                                                                                                                                                                                                                   |
| Monthly Draw Executor (15th, 12:00)   | `Jobs/RunMonthlyDrawExecution` (T-019) wraps `Actions/Draw/ExecuteMonthlyDraw`; scheduled `monthlyOn(15, '12:00')` Asia/Kolkata; on completion, fires a `DrawResultPublished` broadcast Event so an already-open Draw page updates without a refresh (`DOMAIN_LOGIC.md` §8.4's "Real-Time Draw Result Update")                                                                                                   |
| EMI Due Processor                     | `Jobs/ProcessEmiDueStatuses` (T-005) — status transitions only, never activates eligibility itself; scheduled daily at 00:05 Asia/Kolkata (T-019)                                                                                                                                                                                                                                                                |
| Pair/Reward Monthly Evaluator (T-007) | `Jobs/EvaluateMonthlyPairMilestones` — iterates every beneficiary with unused `pair_entries` and runs `EvaluatePairMilestones` per member; guarded by `pair_reward_transactions`' unique constraint; scheduled `lastDayOfMonth('23:30')` Asia/Kolkata (T-019)                                                                                                                                                    |
| Booster Payout Processor              | `Jobs/ProcessBoosterPayouts` (T-011) — credits every due `pending` schedule row through `WalletLedgerService::credit()`, guarded by the `booster_payout_schedules` unique constraint plus a `lockForUpdate()` status re-check for idempotency; scheduled daily at 00:45 Asia/Kolkata (T-019, not monthly — each schedule row's own `scheduled_date` follows the member's individual activation-date anniversary) |
| Payment Webhook Processor             | A controller endpoint that verifies the signature, then dispatches a `PaymentConfirmed` Event — listeners trigger Level Income / EMI status update / Pair Entry creation, decoupled from the webhook response itself                                                                                                                                                                                             |
| Store Profit Processor                | Fired from a `StoreSaleConfirmed` Event the same way (T-014's `CalculateStoreProfitDistribution`)                                                                                                                                                                                                                                                                                                                |
| Report Export Worker                  | `Jobs/ProcessReportExport` (T-018) — queued per export request via `Actions/Reports/RequestReportExport`, never generated synchronously on the request thread; resolves rows via `App\Services\ReportCatalog`, writes csv/xlsx/pdf to the private `local` disk, closes the `report_exports` row out as `ready`/`failed`. "On demand" per its own §19 row, so nothing recurring is registered for it in `App\Console\Scheduling` — there is no cadence to wire, only a queue worker (`php artisan queue:work`) that must run continuously in production, a deployment decision (see Status above)                                                                                                                                                             |
| Store Wallet Processor                | Runs inline inside the same DB transaction as the store sale/joining confirmation (not a separate async job) — DOMAIN_LOGIC.md §16.1 requires the deduction and the payment/purchase record to commit atomically                                                                                                                                                                                                 |
| Purchase/Repurchase Income Processor  | Listener on `StoreSaleConfirmed`, same as Store Profit Processor (T-014's `CalculatePurchaseRepurchaseIncome`)                                                                                                                                                                                                                                                                                                   |

**Concurrency-safety testing (T-020):** the Booster Payout Processor above, plus `StoreWalletService::deduct()` and `ConfirmStoreSale`'s stock check, are each `lockForUpdate()`-guarded critical sections proven safe under genuine parallel access by `tests/Concurrency/` — see the Status section above and `DOMAIN_LOGIC.md` §21's T-020 entries for the full design and findings.

**Scheduler wiring (T-019):** all 6 recurring jobs above are registered in `App\Console\Scheduling::register()`, called from `bootstrap/app.php`'s `->withSchedule(...)`. Every entry sets an explicit `->timezone(config('app.timezone'))` (never left to an implicit default — this project already found a real bug from an unset timezone once, T-003's `phpunit.xml` gap) and `->withoutOverlapping()` (T-019's "retry-safety" requirement — a run still executing when the next tick fires must never start a second concurrent copy; each underlying Job's `handle()` is independently idempotent regardless, so a retried run after a blocked overlap is also always a safe no-op). The registration logic lives in its own class rather than inline in the `bootstrap/app.php` closure specifically so it's directly unit-testable against a plain `new Schedule()` instance (`tests/Feature/Jobs/ScheduledJobsTest.php`) — resolving `Schedule::class` through the container only populates it via Laravel's `Artisan::starting` mechanism, which is unreliable to assert against directly in a test process that runs multiple Artisan invocations (RefreshDatabase's own internal `migrate` call plus anything a test does itself can double-register events in a way specific to that test process, not to how the scheduler behaves in production).

**Deployment requirement — a real OS cron entry, not `schedule:work` alone:** `EvaluateMonthlyPairMilestones`'s `->lastDayOfMonth('23:30')` computes the day-of-month via `Carbon::now()->endOfMonth()->day` once when the schedule registers, which only lands on the correct day every month if `App\Console\Scheduling::register()` re-runs fresh on every check — true for Laravel's documented production setup (`* * * * * php artisan schedule:run` via a real system cron, a new PHP process every minute, so the whole app — and this registration — reboots fresh each time) but **not** true for the long-running `schedule:work` convenience command (intended for local development only), which would freeze this job's day-of-month at whatever it was when that one process started. Whoever sets up deployment (not yet decided, `Docs/DEPLOYMENT.md`) must wire a real cron entry, not rely on `schedule:work` in production.

**Real-time broadcast driver:** not yet chosen — recommend **Laravel Reverb** (first-party, self-hosted, no third-party account needed) over Pusher/Ably since it needs no external vendor relationship; final choice depends on hosting (a WebSocket-capable process needs to run continuously — revisit once `Docs/DEPLOYMENT.md` environment is decided). This is a technical choice, not a business one, so it is _not_ logged in `Docs/DOMAIN_LOGIC.md`'s open-items list — it can be swapped without any business-rule impact. `Events/DrawResultPublished` (T-010) already implements `ShouldBroadcast` against this future driver; with today's `BROADCAST_CONNECTION=log` default it safely logs instead of pushing, so no rework is needed once Reverb is installed.

**Payment gateway / payout provider:** deliberately not chosen here — this is a vendor/cost decision for the user, not an architecture default I should pick. The Action layer (`ConfirmOnlinePayment`, `ProcessPayoutRequest`) should depend on a small interface (`PaymentGatewayContract`, `PayoutProviderContract`) so whichever provider is chosen later plugs in without touching compensation logic.

**Larastan/Carbon tooling note (discovered T-005, 13-09-2026):** on this project's Laravel 13.31 + Larastan 3.11 pairing, Larastan does not reliably infer a `datetime`/`date`-cast Eloquent attribute (declared via the `protected function casts(): array` method style) as `Carbon` — it falls back to treating it as `string`, so calling a Carbon method directly on it (e.g. `$member->activated_at->copy()`) fails static analysis even though it works correctly at runtime. Wrap the first access in `Illuminate\Support\Carbon::parse(...)` (safe regardless of whether the value is already a Carbon instance or a raw string) rather than reaching for `@phpstan-ignore`/a baseline entry. Every future Action/Controller touching a date-cast column for the first time (T-006 onward) should expect this and use the same pattern.

## Permissions / Security Structure

Three roles enforced server-side (`DOMAIN_LOGIC.md` §2), mapped to concrete Laravel primitives:

| Role                  | Middleware / Gate                                                      | Policy scoping                                                                                                                                                                                                                                                                                                                                     |
| --------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `member`              | `EnsureRole:member` on all Member Portal routes                        | Can only view/act on their own `members` row and its relations — enforced in every Policy's `view`/`update` via `$member->user_id === auth()->id()`                                                                                                                                                                                                |
| `admin` (Store Owner) | `EnsureRole:admin` + `EnsureStoreOwnership` on all Admin/Store routes  | Every Store-scoped query is additionally constrained to `stores.owner_user_id === auth()->id()`; `StorePolicy`, `StoreSalePolicy`, etc. deny anything outside the assigned store. Company-wide member/compensation/draw/payout/settings routes are not registered under the Admin route group at all (defense in depth beyond just a Policy check) |
| `super_admin`         | `EnsureRole:super_admin` on Super Admin routes; otherwise unrestricted | Can additionally open any member's Directs/Tree view and any store — Policies allow `super_admin` to bypass the ownership check explicitly, not by omitting the check                                                                                                                                                                              |

`users.role` (added in T-002) is the source of truth for role checks — never inferred from which route was hit or from the presence of a `members` row (a `members` row can exist for a dummy/unassigned entry with no `users` row at all).

## Change Rules

- Update this file when the folder structure, layering rule, or job/event wiring actually changes in the code — keep it in sync with `app/`, don't let it drift into aspiration.
- Business rules stay in `DOMAIN_LOGIC.md`; this file only says where the code implementing them lives.
- Vendor/provider choices (payment gateway, payout provider, broadcast driver, hosting) are tracked here as open technical decisions, not duplicated in `DOMAIN_LOGIC.md`'s business-open-items list.
