# Current Handoff

Keep only information needed to continue current work. Move durable decisions to their canonical document; remove stale entries when the task closes.

## Snapshot

| Field         | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Updated       | 15-09-2026                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Active task   | None — T-020 implemented and fully verified today; stopping here per standing instruction. All feature tasks and the risk-based test coverage pass are now complete — the full GoldWave backlog is done. |
| Current state | T-001–T-021 complete, including T-020. T-020 (automated test coverage, concurrency focus per the user's explicit instruction) added a new `tests/Concurrency/` PHPUnit testsuite (`phpunit.concurrency.xml`, real dev Postgres, excluded from the default `php artisan test` run since SQLite `:memory:` can't produce a genuine row-level-lock race) proving 3 financial critical sections stay correct under real simultaneous OS-process access — Store Wallet deduction, Store Inventory decrement, Booster Payout Processor — each via a tiny test-worker artisan command (`app/Console/Commands/Concurrency*.php`) and a test asserting the database's exact final state, non-flaky across repeated runs. Building these surfaced one real schema gap (not a locking bug — the locking itself is correct): `booster_payout_schedules.wallet_ledger_entry_id` had no `ON DELETE` action, fixed with a new migration (`ON DELETE SET NULL`). Also audited `Docs/TEST.md`'s full Risk-based Coverage table row-by-row and filled 2 real, narrow gaps in already-existing tests (a Sponsor-vs-Binary-Position divergence proof for Level Income plus a rounding-correctness test, and a symmetric Right-side occupied-traversal test for binary placement — the existing one only covered Left). Verified: 206/206 default Pest suite, 3/3 concurrency suite (non-flaky across 3 runs), Pint clean, Larastan level 7 0 errors, `npm run build`/`types:check` clean, zero leftover fixtures in the real dev database. See `Docs/TASKS.md` T-020's Completed entry for the full detail. Previously, T-018 (Reports module) built a new, separate, Super-Admin-only Reports Center covering all 11 `INSTRUCTIONS.md` "Reports" report types (does not touch M17/A06, which stay their own small/synchronous/own-scope exports) — `report_exports` tracking table, `Actions/Reports/RequestReportExport`, `Jobs/ProcessReportExport` (genuinely queued, `QUEUE_CONNECTION=database`), `App\Services\ReportCatalog` (11 `cursor()`-backed query methods), xlsx via `maatwebsite/excel`, pdf via `barryvdh/laravel-dompdf`, `SuperAdmin\ReportsController`, new `resources/js/pages/super-admin/reports.tsx` + sidebar section, and a new `App\Support\Dates::display()` (DD-MM-YYYY for server-generated files, since the frontend `formatDate()` pass never runs on them). Verified: 203/203 Pest, Pint clean, Larastan level 7 0 errors, `npm run build`/`types:check` clean; full manual browser QA with real `php artisan queue:work` processing across all 3 formats, **independently re-verified in a second pass** (the code was spot-checked line-by-line against the actual model schemas, and a fresh export request was made and processed and its file content read directly off disk) before sign-off — no discrepancies found. See `Docs/TASKS.md` T-018's Completed entry for the full detail. Previously, T-017 (Super Admin Portal, S01–S10 + Admin Dashboard/Member/Compensation/Draw management) built 13 new `Http/Controllers/SuperAdmin/*` controllers, 5 new backend Actions (`PublishRuleVersion`, `SetMetalRate`, `CreateAdminUser`, `ReassignStoreOwner`, `ReconcileDrawExecution` — almost none of Super Admin's write-paths existed before this task), and 16 new `resources/js/pages/super-admin/*.tsx` pages behind a new `super-admin-layout.tsx`/`super-admin-sidebar.tsx` shell; the top-level `DashboardController` gained a `role === 'super_admin'` branch (S01), completing the 3-way M01/A01/S01 dashboard split. **This task's own manual QA pass was interrupted mid-way by the session hitting its usage limit — the first real test of the user's explicit "checkpoint progress so a cutoff never forces rework" instruction, and it worked as intended:** progress was written to this file's Active task row at each sub-part boundary (pre-coding done → backend Actions done → controllers/pages done → QA in progress with an exact per-screen confirmed/unconfirmed list), so the resumed session re-verified the automated suite once, then continued QA exactly from the unconfirmed point (S09's store detail page onward) rather than re-testing or rebuilding anything already done. **Full manual QA across all 16 screens, with several real writes, found and fixed 3 real issues, none a business-rule question** — full detail in `Docs/DOMAIN_LOGIC.md` §21's T-017 build & QA entry: a Member Detail "Recent Activity" feed sort-order bug (sorting already-formatted date strings instead of the underlying timestamp), S04's dummy-entry count double-counting the seeded company-root Member, and a raw UUID shown as the most prominent text in the Store Wallet ledger row. See `Docs/TASKS.md` T-017's Completed entry for the full verification list (195/195 tests, Pint, Larastan level 7, `npm run build`/`types:check`, full manual browser walkthrough with real writes). Previously, T-016 (Admin/Store Owner Portal, A01–A06) built 5 `Http/Controllers/Admin/*` controllers plus a `store-owner` `EnsureStoreOwnership` middleware and a `DashboardController` branch delegating to a new `StoreDashboardController` (mirroring M01's member branch), all calling straight through to the T-014 Store Actions already built; a new `admin-layout.tsx`/`admin-sidebar.tsx` shell wraps every `admin/*` page. **Did a full manual browser QA pass, not just the automated suite** — logged in as a real Store Owner against the real Postgres dev DB and clicked through all 6 pages, including submitting a real sale through the browser form. **That pass found three real issues, all fixed, none a business-rule question:** (1) a genuine, previously-unimplemented §16.3 requirement — no T-014 Store Action was writing `store_activity_logs` rows — fixed with a new `App\Services\StoreActivityLogger`; (2) a Card-list item-row visual overlap affecting `admin/inventory.tsx` and 2 pre-existing T-015 pages sharing the same layout pattern (`member/level-income.tsx`, `member/wallet.tsx`) — fixed uniformly; (3) a much larger gap — the Indian DD-MM-YYYY date format (hard requirement since T-003) had never actually been applied anywhere in the running app, affecting every dynamic date across both the Member and Admin portals — fixed app-wide, with the user's explicit go-ahead, via one new shared `formatDate()` helper applied at all 13 affected page files. Full detail in `Docs/DOMAIN_LOGIC.md` §21's new T-016 log. See `Docs/TASKS.md` T-016's Completed entry for the full verification list (174/174 tests, Pint, Larastan level 7, `npm run build`/`types:check`, full manual browser walkthrough). |
| Next owner action | **None — the entire `Docs/TASKS.md` backlog is now complete.** Every feature task (T-001 through T-021) is done and verified. Any future work would be a new, explicitly-requested task, not a continuation of this backlog. |
| Blocker / decision needed | **No hard blockers remain.** 3 lower-stakes open items, none blocking: the Withdrawal Processing Fee's flat-₹-vs-percentage shape (T-009, default 0%, inert), whether an assigned MLM leader needs to select a plan and pay (T-013, currently no plan/payment required), and 2 store-related numbers expected to be tuned later — the Item Buyback percentage's exact final value (T-021/T-014, client-stated default 60%) and the store GST/tax percentage's exact statutory value (T-014, default 0%, inert until confirmed). Everything else is resolved, but per the user's standing instruction, resolution does not equal permission to code — wait for explicit go-ahead on each task. |

## Latest Handoff

### 15-09-2026 — T-020: Add automated test coverage per risk-based coverage table (concurrency focus per user request)

- Done: The user explicitly asked to focus on concurrency. Audited the existing 203-test suite first and found every module already had solid *sequential* idempotency coverage, but zero genuine parallel/race-condition test existed anywhere — confirmed by grep, no test used more than one DB connection or a second process. `Docs/TEST.md` scenario 15's own regression note was factually wrong, referring to "the existing Store Wallet concurrent-deduction test" that never actually existed.
- **A true concurrency test can't run against the default Pest suite.** SQLite `:memory:` is connection-local (a second connection can't even see the same database) and the `goldwave` DB role has no `CREATEDB` privilege for a scratch Postgres database. Built a new, separate `tests/Concurrency/` PHPUnit suite (`phpunit.concurrency.xml`, its own config since PHPUnit's `<php>` env block is global — can't mix DB connections within one config file) that runs against the real dev Postgres database, excluded from the default `php artisan test` run, using real separate OS processes (`Illuminate\Support\Facades\Process::start()`) to produce genuine races — not in-process simulation, which is impossible since PHP is single-threaded per process.
- Built and verified 3 concurrency tests, exactly the scenarios `Docs/TEST.md`'s risk table names by name: **Store Wallet deduction** (3 processes race to deduct from a throwaway wallet, confirmed exactly 2 of 3 succeed and the final balance is mathematically exact), **Store Inventory decrement** (same pattern against tracked stock), **Booster Payout Processor** (3 simultaneous scheduler-tick simulations race for the same due schedule, confirmed it's paid exactly once, never zero, never twice). Each backed by a tiny, single-purpose `concurrency:*` artisan command that exists purely as a test worker, never user-facing. All 3 pass reliably across repeated runs (no flakiness) — the underlying `lockForUpdate()` critical sections in `StoreWalletService`, `ConfirmStoreSale`, and `ProcessBoosterPayouts` are all confirmed genuinely safe under real parallel access, not just sequential retries.
- **Building the Booster Payout test's cleanup surfaced a real, separate bug — not a concurrency/locking bug, a schema gap.** `booster_payout_schedules.wallet_ledger_entry_id` had no `ON DELETE` action at all, so deleting a Member with a *paid* booster schedule could fail with a foreign key violation (two independent cascade paths from the same `members` row could race against each other in Postgres's own internal FK-action ordering). Fixed with a new migration setting that one FK to `ON DELETE SET NULL` — verified via both `migrate` and `migrate:rollback` against the real dev database.
- **Also audited the risk table's remaining 6 non-concurrency rows individually** (not assumed covered by proximity — actually read each relevant test). Found and filled 2 more real, narrow gaps: Level Income had never actually tested "never falls back to Binary Position chain" with a genuine divergence scenario (added one, with a decoy member reachable only via placement, confirmed it gets zero income) or a rounding case that doesn't divide evenly (added one, ₹333 × 0.5% = 1.665 → confirmed rounds to 1.67); Binary placement's occupied-side-traversal-at-depth test only covered the Left side, so added a symmetric Right-side version. Confirmed Pair/Reward, Payment webhook idempotency, and Payout processing were already thoroughly covered — no gap, no padding added. Confirmed Purchase/Repurchase's "duplicate-beneficiary" rule is a structural guarantee of the tree-shaped Sponsor chain resolver, not independently testable beyond existing coverage — no test added for an unconstructable input state. Confirmed "Scheduled jobs generally" is adequately covered through each job's own domain-level idempotency test plus the separate scheduling-wiring test.
- Evidence: `php artisan test` — 206/206 passing (203 + 3 new coverage-audit tests); `php artisan test` with the `configuration` option set to `phpunit.concurrency.xml` — 3/3 passing, confirmed non-flaky across 3 repeated runs; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean; confirmed zero leftover throwaway fixtures in the real dev Postgres database after every concurrency-suite run. `Docs/ARCHITECTURE.md`/`Docs/DOMAIN_LOGIC.md` (§21, two new T-020 entries — pre-coding and build & QA)/`Docs/TEST.md` (new "Concurrency Verification" section, corrected scenario 15's stale regression note) all updated to match what was actually built and discovered.
- Next: none — this was the last item in `Docs/TASKS.md`'s backlog (T-001 through T-021 all complete).
- Blocked by: none. No further backlog items remain; any future work is a new, separately-scoped request.

### 14-09-2026 — T-018: Build Reports module (all report types + queued exports)

- Done: Built a new, separate, Super-Admin-only Reports Center covering all 11 `INSTRUCTIONS.md` "Reports" table rows (Membership, EMI, Level Income, Pair/Reward, Draw, Booster, Payment In, Payment Out, Wallet/Ledger, Store Sales, Store Distribution) — does not touch M17 (Member's own small/synchronous reports, T-015) or A06 (Admin's own store-scoped reports, T-016), per §2's "no company-wide access" rule for Admin. New `App\Services\ReportCatalog` holds one `cursor()`-backed query method per report type against the already-existing models — no new report-data tables, only a new `report_exports` tracking table.
- Every export is queued uniformly through a new `Actions/Reports/RequestReportExport` + `Jobs/ProcessReportExport implements ShouldQueue`, rather than building row-count threshold-detection logic to decide "small vs. large" — a small result set just finishes near-instantly once a worker picks it up. `QUEUE_CONNECTION=database` was already configured in `.env` but unused until this task. CSV is written with proper `fputcsv()` escaping (not the naive `implode(',', ...)` pattern M17/A06 used, which never escapes a comma inside a name/description field); xlsx via a new package `maatwebsite/excel`; pdf via a new package `barryvdh/laravel-dompdf`. A new `App\Support\Dates::display()` formats dates DD-MM-YYYY directly for these server-generated files, since the frontend `formatDate()` pass (T-016's app-wide fix) never runs on them.
- **Did the required full pre-coding pass before any code** (`Docs/DOMAIN_LOGIC.md` §21's new T-018 entry, `Docs/TEST.md` scenario 18): resolved the M17/A06-vs-T-018 scope boundary, the uniform-queueing design, the generic cross-report filter set (date range + optional customer_id/store_id), and all 11 report-type-to-data-source mappings directly from existing text, none needing to ask the user. One genuine spec gap resolved without inventing a number: Wallet/Ledger's "balance" column has no backing running-balance data anywhere in the schema — omitted rather than fabricated, showing the member's current balance alongside each row instead.
- **Did the full manual browser QA this system prompt requires for UI work, not just the automated suite, including actually running the queue worker** — logged in as the real Super Admin against the real Postgres dev database, requested exports across all 3 formats (Payment In PDF, Store Sales XLSX, Membership CSV) through the browser, then ran `php artisan queue:work --stop-when-empty` against the real `QUEUE_CONNECTION=database` queue exactly as a production worker would, confirming each completed `ready` with the correct row count and correct, downloadable file content.
- **Independently re-verified the build's own QA claims before sign-off** (this task was built via a delegated background pass, per this project's now-established pattern for large mechanical builds) — re-ran the full automated suite, read the `ReportsController`/`RequestReportExport`/`ProcessReportExport`/`ReportCatalog` source directly and cross-checked every model column/relation it uses against the real model files (all confirmed correct — `WalletLedgerEntry.processed_at`, `EmiInstallment::payment()`, `StoreSale.sale_amount`/`total_invoice_amount`, etc.), then made one more fresh export request through a real browser session and read its generated file directly off disk. No discrepancies found; no real bugs surfaced beyond what the build pass had already caught and fixed itself.
- Evidence: `php artisan test` — 203/203 passing (8 new tests in `tests/Feature/SuperAdmin/ReportExportTest.php` — see `Docs/TASKS.md` T-018's entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean; full manual browser walkthrough with real queue processing across all 3 export formats. `Docs/ARCHITECTURE.md`/`Docs/DOMAIN_LOGIC.md` (§21, new T-018 entry) updated to match what was actually built.
- Next: T-020 (automated test coverage per the risk-based coverage table) is the only remaining backlog item. Awaiting user go-ahead before starting it.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 14-09-2026 — T-017: Build Super Admin pages S01–S10 + Admin Dashboard/Member/Compensation/Draw management

- Done: Built the full Super Admin Portal — S01 (System Dashboard, delegated to from the shared `/dashboard` route the same way M01/A01 are), S02 (Admin Users & Permissions), S03 (Compensation Rule Versions), S04 (Daily Dummy Entry Settings, plus a manual "Generate Now" trigger), S05 (Dummy Entry Assignment, thin wrapper over T-013's existing `AssignDummyEntryToLeader`), S06 (Draw Master Settings + Admin Draw Management/reconciliation), S07 (Gold & Silver Rate Settings), S08 (Payout & TDS Settings), S09 (Store Management — create/reassign-owner/status, reusing T-014's `CreateStore`), S10 (Store Wallet Management, reusing T-014's `RecordStoreWalletTopup`), Admin Member Management (list + detail with a synthesized "Recent Activity" feed), Admin Compensation Management (config + calculation audit). New `super-admin-layout.tsx`/`super-admin-sidebar.tsx` shell (6 grouped nav sections, 13 items) wraps every `super-admin/*` page.
- Unlike T-015/T-016 (which mostly wrapped Actions already built), almost none of Super Admin's write-paths existed before this task — only reads/one-time seeding. Built 5 new Actions: `Actions/Settings/PublishRuleVersion` (one shared writer behind every versioned-settings page — S03/S04/S06/S08/Admin-Compensation-config — rather than 5 near-duplicate Actions, since the versioning model requires any settings change to publish a whole new `RuleVersion`, never mutate an existing one in place), `Actions/Settings/SetMetalRate` (S07), `Actions/Admin/CreateAdminUser` (S02), `Actions/Store/ReassignStoreOwner` (S09), `Actions/Draw/ReconcileDrawExecution` (S06's reconciliation half — 2 new `draw_executions` columns + a new `draw_execution_corrections` audit table, since §8.6 requires "a reversal/correction audit record" that never overwrites draw history).
- **Did the required full pre-coding pass before any code** (`Docs/DOMAIN_LOGIC.md` §21's new T-017 pre-coding entry): 5 architecture decisions resolved directly from already-established text, none needing to ask the user — the shared `PublishRuleVersion` design; Admin-user-creation and store-owner-assignment as genuinely separate steps; S06's reconciliation resolving to a minimal status-close-out design rather than an unspecified re-pick/void mechanic; Member Detail's "audit/activity history" as a synthesized read-side feed, not a new audit-log table; the new `storeProfitDistributions()` relation. One genuine spec gap flagged rather than guessed: `IncomeLedgerCalculation`'s missing reversal/reference column — no rule describes reversing a finalized compensation calculation, so very likely inert by design.
- **Did the full manual browser QA this system prompt requires for UI work, not just the automated suite, across all 16 screens, with several real writes** — logged in as the real seeded Super Admin against the real Postgres dev database and exercised a compensation rule-version publish, an Admin user creation, a metal rate entry, a dummy-entry-leader assignment, and a full draw reconciliation with a correction note, confirming each round-trips correctly with real data. Cleaned up all throwaway test data (a store, an Admin user, 5 test Members, a draw group/executions) and stopped the dev server afterward.
- **This QA pass found 3 real issues, all fixed, none a business-rule question — full detail in `Docs/DOMAIN_LOGIC.md` §21's new T-017 build & QA log:**
  1. **A Member Detail "Recent Activity" feed sort-order bug:** the synthesized feed was sorting on already-DD-MM-YYYY-formatted date *strings* (alphabetical, not chronological) instead of the underlying timestamp. Fixed by sorting on the raw `Carbon` value first, formatting only after — extending this project's "format at display time only" rule to also mean "format after, not before, any sort/comparison."
  2. **S04's "Generated" dummy-entry count double-counted the seeded company-root Member** (`is_company_root=true` is also flagged `is_company_dummy=true`, purely so the existing generation chain-traversal logic can anchor to it). Excluded the root explicitly from the stats query.
  3. **A raw 36-character UUID displayed as the first, most prominent text in the Store Wallet ledger row.** `StoreWalletLedgerEntry.reference` is a real, required §16.1 "immutable reference" — correctly present in the data — but showing it ahead of the human-readable description made every row unreadable. Fixed by moving it after the description and truncating to 8 characters, no backend change.
- **This task's own QA pass was itself interrupted mid-way by the session hitting its usage limit — the first real test of the user's explicit instruction (given at the start of this task) to checkpoint progress so a cutoff never forces rework, and it worked as intended.** Progress was written to this file's Active task row at each sub-part boundary as it completed (pre-coding pass done → 5 backend Actions done and tested → 13 controllers/16 pages done and tested → QA in progress, with an exact list of which of the 16 screens were confirmed-working vs. still unconfirmed). When the resumed session picked this back up (after the usage-limit reset), it re-verified the already-passing automated suite once, then continued QA exactly from the documented unconfirmed point (S09's store detail page onward) — it did not re-test S01/S02/S03/S09-index, which were already confirmed, and did not rebuild or re-derive any already-finished backend/controller/page code.
- Evidence: `php artisan test` — 195/195 passing (21 new tests across `tests/Feature/SuperAdmin/SuperAdminSettingsTest.php` and `SuperAdminPortalTest.php` — see `Docs/TASKS.md` T-017's entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean; full manual browser walkthrough of all 16 screens with several real writes and zero console errors. `Docs/ARCHITECTURE.md`/`Docs/DOMAIN_LOGIC.md` (§21, two new T-017 logs — pre-coding and build & QA) updated to match what was actually built and discovered.
- Next: T-018 (Reports) is the natural next step, now unblocked (all 3 UI tasks complete). T-020 (test coverage) can run alongside it. Awaiting user go-ahead before starting either.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 14-09-2026 — T-016: Build Admin/Store Owner pages A01–A06

- Done: Built the full Admin/Store Owner Portal — A01 (Store Dashboard, delegated to from the shared `/dashboard` route the same way M01 is), A02 (Store Profile — `contact`/`location` only editable, everything else read-only per §16.1/§17.2), A03 (Repurchases/Sales — new sale, Item Buyback, Plan Jewellery Delivery, all reusing T-014's Actions untouched), A04 (Inventory, read-only), A05 (Store Transactions), A06 (Store Reports, CSV exports). Every controller is thin, calling straight through to T-014's Store Actions — no business logic duplicated. New `EnsureStoreOwnership` middleware (alias `store-owner`) resolves the one `Store` a `role=admin` user owns and 403s if none. New `resources/js/layouts/admin-layout.tsx` + `admin-sidebar.tsx` (mirroring the Member Portal's shell) wrap every `admin/*` page.
- 4 architecture/access-control decisions resolved directly from already-established `DOMAIN_LOGIC.md` text, none needing to ask the user: Admin/Super Admin login stays the untouched generic Fortify `/login` (the source spec never defines a dedicated mechanism); A02's editable-fields boundary (contact/location only); A03's "new joining payments" interpreted as `RecordPlanJewelleryDelivery`; a Store Owner's dual `role=admin`+linked `members` row is purely so `Store::ownerMember()` resolves for Store Profit Distribution's owner-share wallet crediting, not a login-mechanism requirement.
- **Did the full manual browser QA this system prompt requires for UI work, not just the automated suite:** started the real dev server, created a throwaway test Admin/Store Owner account owning a real store with real inventory (via `php artisan tinker`), logged in through the real `/login` flow against the real Postgres dev database, and clicked through all 6 pages — including submitting a real sale through the browser form and confirming the invoice/activity-log/recent-transactions update. Cleaned up the throwaway store/admin/inventory/sale data and stopped the dev server afterward.
- **That manual pass found three real issues, all fixed, none a business-rule question — full detail in `Docs/DOMAIN_LOGIC.md` §21's new T-016 log:**
  1. **A genuine, previously-unimplemented §16.3 requirement:** no T-014 Store Action was writing `store_activity_logs` rows at all, despite the schema and DOMAIN_LOGIC.md both describing it since T-014. Fixed with a new `App\Services\StoreActivityLogger`, called from every Store Action after its own write, plus a new regression test asserting it.
  2. **A Card-list item-row visual overlap:** a two-line left text block crowded against a bare-text right value with no `gap` and `items-center` vertical-centering, found on `admin/inventory.tsx`'s stock/buyback rows and 2 pre-existing T-015 pages sharing the exact same markup pattern (`member/level-income.tsx`, `member/wallet.tsx`). Fixed uniformly with `items-start`/`gap-3`/`shrink-0 whitespace-nowrap`; rows whose right side is already a `Badge`/`Button` (their own box padding prevents the same crowding) were left untouched.
  3. **A much larger gap the same QA pass surfaced:** the Indian DD-MM-YYYY date format — a hard "must" requirement recorded in AGENTS.md since T-003 — had never actually been applied to any dynamic date anywhere in the running app. `App\Support\Dates::date()` (used by every T-015/T-016 controller) only ever produced plain ISO `YYYY-MM-DD`, built purely as a Larastan/Carbon workaround, never for display formatting; no frontend date-formatting helper existed at all. Since this affected every already-completed T-015 page too, not just T-016's new ones, asked the user how to scope the fix rather than silently either ignoring it or unilaterally sweeping the whole codebase — user chose to fix it app-wide now, in this same checkpoint. Fixed with one new shared `formatDate()` helper in `resources/js/lib/utils.ts` (regex-converts a leading `YYYY-MM-DD` to `DD-MM-YYYY`; returns `'—'` for null; leaves non-ISO strings like a `YYYY-MM` month value untouched), applied at every dynamic-date display site across 13 Member Portal and Admin Portal page files. No backend/database change was needed — formatting is applied purely at display time. Re-verified visually in the real browser (Store Transactions' Operational History log correctly showing `14-09-2026`) and via a clean `npm run types:check` before the final rebuild.
- Evidence: `php artisan test` — 174/174 passing (10 new tests in `tests/Feature/AdminPortal/AdminPortalTest.php` — see `Docs/TASKS.md` T-016's entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean; full manual browser walkthrough of all 6 pages with zero console errors. `Docs/ARCHITECTURE.md`/`Docs/DOMAIN_LOGIC.md` (§21, new T-016 log) updated to match what was actually built and discovered.
- Next: T-017 (Super Admin UI) is the natural next step, unblocked. Awaiting user go-ahead before starting it.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 14-09-2026 — T-015: Build Member Portal pages M01–M18

- Done: Built the full Member Portal — M01 (Dashboard, the shared `/dashboard` route branching on role), M02-M05 (Profile/Pending Profile/Change Requests/Membership Plan), M06 polish (EMI's paid date/reference/mode columns + Pair/Reward eligibility indicator, both deferred at T-005), M07 (Payment History), M10-M12 (Level Income/Pair-Reward/Booster), M13-M16 (Monthly Draw/Wallet/Payout), M17 (own-data CSV Reports — the full cross-role report catalog is T-018's job), M18 (Notifications). Every controller is thin: validate via a Form Request, call the Action T-006–T-014 already built, render the page — no business logic duplicated anywhere in the controller layer. New `resources/js/layouts/member-layout.tsx` + `member-sidebar.tsx` replace the generic starter-kit `AppLayout` for every `member/*` page, grouping all 18 nav items (Overview/Profile/Membership & Payments/My Network/Income/Draw & Wallet/More) — M08/M09 (Directs/Tree View, T-004) now render inside this same shell instead of the generic one they'd been sitting in.
- Given the size of this task (18 pages), asked the user how to pace it — they chose to do all 18 in one pass rather than splitting into sub-checkpoints, matching the standing per-task (not per-page) checkpoint instruction.
- **Did the full manual browser QA this system prompt requires for UI work, not just the automated suite:** started the real dev server, created a throwaway test member, logged in through the actual `/member/login` flow against the real Postgres dev database, and clicked through all 18 pages plus M08/M09, checking rendered data and the browser console after each. Cleaned up the throwaway member/user afterward.
- **That manual pass surfaced a real, significant bug the automated test suite structurally could not have caught:** `RuleVersionSeeder`'s guard was `if (any active RuleVersion exists) return;` — a blanket per-version check, not per-key — so once the real dev database was seeded during T-003, every `php artisan db:seed` run after that (across T-006 through T-014, each of which added new `rule_values` keys) silently did nothing. The Income Booster page's level cards were rendering empty despite `RuleVersionSeeder` clearly defining 3 levels; investigating why led to discovering the real dev database's active rule version had exactly 1 key (`emi_current_rate_maintenance_cost_percent`, from T-003) instead of the 17 the seeder now defines — every other feature (booster thresholds, pair milestones, payout minimums, draw group size, purchase/repurchase and store distribution rates, item buyback/GST percentages) had been silently running on inert defaults in the real dev app this whole time. `php artisan test` could never have shown this, since it always migrates and seeds a fresh SQLite database on every run. Fixed at the root: the seeder now does `RuleVersion::firstOrCreate(...)` for the version and a per-key `firstOrCreate(['key' => $key], ['value' => $value])` for every setting, so re-running the seeder against any existing database safely backfills whatever's missing. Ran it once against the real dev database to backfill all 16 missing keys — purely additive, verified no existing member/registration/store data was touched.
- Evidence: `php artisan test` — 163/163 passing (10 new tests in `tests/Feature/MemberPortal/MemberPortalTest.php` — see `Docs/TASKS.md` T-015's entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (a Larastan/Carbon date-cast gap recurred across a dozen new controllers — fixed once via a new shared `App\Support\Dates::date()` helper instead of repeating the workaround inline everywhere); `npm run build`/`types:check` clean; full manual browser walkthrough of all 18 pages with zero console errors. `Docs/ARCHITECTURE.md`/`Docs/INSTRUCTIONS.md`/`Docs/DOMAIN_LOGIC.md` (§21, new T-015 log) updated to match what was actually built and discovered.
- Next: T-016/T-017 (Admin/Super Admin UI) are the natural next step, both unblocked. Awaiting user go-ahead before starting either.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-019: Wire scheduled jobs (draw grouping/execution, dummy generator, booster payout, EMI due processor)

- Done: Registered all 6 recurring `DOMAIN_LOGIC.md` §19 jobs onto Laravel's scheduler: Daily Company Direct Generator (daily, 00:30), Draw Group Generator (15th, 00:00), Monthly Draw Executor (15th, 12:00), EMI Due Processor (daily, 00:05), Pair/Reward Monthly Evaluator (last day of month, 23:30), Booster Payout Processor (daily, 00:45) — all Asia/Kolkata, all `withoutOverlapping()`. Built 2 missing Job wrappers (`RunDrawGroupGeneration`, `RunMonthlyDrawExecution`) since T-010's `GenerateDrawGroups`/`ExecuteMonthlyDraw` Actions had none yet, unlike every other §19 job which already had one from its own originating task.
- 4 architecture decisions resolved directly, none needing to ask the user (`DOMAIN_LOGIC.md` §21/§19): the source spec left several jobs' exact time-of-day unspecified ("exact time TBD" for the Dummy Generator, a bare "Daily"/"Scheduled" for the EMI Due Processor/Booster Payout Processor) — in every case the _day_ was already fully determined by existing rules, only the _time_ was open, a purely operational choice. The one non-trivial call: the Booster Payout Processor runs **daily**, not monthly, because each `booster_payout_schedules` row's own due date follows the member's individual activation-date anniversary (T-011's own precedent) — a single monthly cutoff would miss members whose due date falls on a different day of the month.
- **Found and worked around a real, non-obvious Laravel testing quirk, not a business-rule issue:** `bootstrap/app.php`'s `withSchedule()` wires its callback through a static `Illuminate\Console\Application::$bootstrappers` array gated on `Artisan::starting`, which behaves unpredictably (can silently double-register events) in a single test process that runs multiple Artisan invocations — e.g. `RefreshDatabase`'s own internal `migrate` call plus a test's own commands. This has no bearing on real production behavior (a fresh PHP process per `schedule:run` cron tick fires it exactly once) but made the wiring genuinely untestable via direct `Schedule::class` container resolution — spent real time diagnosing it via `php artisan tinker`/`schedule:list` before finding the cause. Fixed properly, not worked around: extracted the actual job registrations out of the `bootstrap/app.php` closure into a new `App\Console\Scheduling::register()` class, directly callable against a plain `new Schedule()` instance in tests, sidestepping the Artisan lifecycle entirely.
- Also documented (no code change): production deployment must use a real per-minute OS cron entry running `php artisan schedule:run`, never the `schedule:work` convenience command — `lastDayOfMonth()` recomputes its target day-of-month fresh only when the whole app reboots on each check, which is true under the documented cron model but not under a single long-running `schedule:work` process. Recorded in `Docs/ARCHITECTURE.md`'s Background Jobs section.
- Evidence: `php artisan test` — 153/153 passing (7 new tests in `tests/Feature/Jobs/ScheduledJobsTest.php` — see `Docs/TASKS.md` T-019's entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean; manually verified via `php artisan schedule:list -v` showing all 6 jobs with correct cron expressions and `+05:30` next-run times. `Docs/ARCHITECTURE.md`/`Docs/DOMAIN_LOGIC.md` updated to match what was actually built.
- Next: every backend task is now complete. T-015/T-016/T-017 (Member/Admin/Super Admin UI) are the natural next step. Awaiting user go-ahead before starting any of them.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-014: Store Management, Store Wallet, Purchase/Repurchase, Invoice, Store Profit Distribution

- Done: Built the full Store module backend per `DOMAIN_LOGIC.md` §15/§16. `CreateStore` (Super Admin opens a store, creates its Store Wallet, credits the initial advance amount, §16.1); `AllocateStoreInventoryItem` (item-wise stock, §16.5); `RecordStoreWalletTopup` + `StoreWalletService` (the only class allowed to write `store_wallet_ledger_entries`/mutate `store_wallets.balance` — mirrors `WalletLedgerService`'s exclusivity rule); `ConfirmStoreSale` (decrements tracked inventory atomically with an insufficient-stock block, deducts the Store Wallet atomically when paid from it, generates the Invoice, dispatches `StoreSaleConfirmed` after its own transaction commits); `RecordItemBuyback` (prices strictly on the _current_ market rate via a passed-in `MetalRate`, restocks inventory, deliberately creates no compensation rows); `RecordPlanJewelleryDelivery` (marks a `product_benefits` row delivered through a store, then calls `ConfirmStoreSale` itself). Two new compensation Actions listen on `StoreSaleConfirmed` independently: `CalculatePurchaseRepurchaseIncome` (self 2% + Sponsor/Direct chain 1-12, no-op without a purchasing member) and `CalculateStoreProfitDistribution` (Owner 2% + the owner's own Sponsor/Direct chain levels 1-3, fires on every confirmed sale including a walk-in one).
- Before writing any code, did the required pre-coding pass (`Docs/DOMAIN_LOGIC.md` §16.8–§16.10/§21's new T-014 log) and resolved 3 architecture decisions directly from already-confirmed text, none needing to ask the user: (1) a sale needs a stock/quantity check exactly mirroring the Store Wallet's existing insufficient-balance-blocks-the-transaction pattern; (2) an Item Buyback structurally cannot trigger Store Profit Distribution or Purchase/Repurchase Income, since both rules only ever describe a _sale_ (money in for jewellery going out) and a Buyback is the reverse direction — outside both rules' own stated scope; (3) §16.4 scenario 3's "new joining with jewellery delivery at a store" is wired via an explicit, optional `RecordPlanJewelleryDelivery` action rather than an automatic side effect of registration, since the scenario's own wording is conditional on delivery actually happening through a store. One genuine spec gap flagged rather than guessed, added to `DOMAIN_LOGIC.md` §21's "Still open" table: the store sale GST/tax percentage's real statutory value is never stated anywhere in the source documents — seeded as a Super-Admin-configurable setting defaulting to 0% (inert), the same pattern already used for the Withdrawal Processing Fee.
- **Found and fixed a real cross-database migration bug while verifying against the real Postgres dev DB, not just the SQLite test suite** (this project's established practice — several earlier tasks caught real issues this way too): Laravel's `enum(...)->change()` generates invalid Postgres syntax when the column already has a named `CHECK` constraint (an inline `CHECK` clause inside an `ALTER COLUMN TYPE` statement, which Postgres's grammar rejects outright) — the SQLite test suite passed fine since SQLite has no equivalent named-constraint conflict, so this would have shipped silently broken against production Postgres if not separately verified there. Fixed with a driver-aware migration: raw SQL to drop and recreate the named constraint on `pgsql`, the normal Schema-builder `->change()` path on `sqlite`.
- Evidence: `php artisan test` — 146/146 passing (11 new tests in `tests/Feature/Store/StoreModuleTest.php` reproducing `Docs/TEST.md` scenarios 15/16/17's exact worked numbers plus re-verifying scenarios 4/5's numbers on the real store-sale path — see `Docs/TASKS.md` T-014's entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — the Store Owner's Inventory/Repurchases/Transactions pages are T-016's job); `php artisan migrate` verified clean against the real Postgres dev DB, including a full rollback/re-migrate of all 4 new migrations to confirm their `down()` methods. `Docs/TEST.md` scenarios 15/16/17 (new) written before the implementation code. `Docs/ARCHITECTURE.md`/`Docs/DATABASE_SCHEMA.md`/`Docs/DOMAIN_LOGIC.md` updated to match what was actually built.
- Next: **T-019 (Wire scheduled jobs)** is the only remaining unblocked backend task. T-015/T-016/T-017 (Member/Admin/Super Admin UI) are also now unblocked. Awaiting user go-ahead before starting either.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-021: Store inventory/repurchase/returns/POS resolved (documentation only)

- Done: User answered the last remaining tracked open item in `Docs/DOMAIN_LOGIC.md` §21 — store inventory management, repurchase limits, returns policy, and POS-level payment details, previously "explicitly deferred to the client" (§17.3). (1) Inventory is tracked item-wise: Item name, Metal, Weight, Quantity, Price, short description. (2) Repurchase has no quantity/weight limit per transaction. (3) No returns/refunds exist anywhere for jewellery (plan or store) — but the client introduced a genuinely new concept while answering: **Item Buyback**, the store buying back a previously-sold item from its owning member at a later date. Asked the user directly whether the buyback price should be based on the original sale price or the current market rate — confirmed current market rate, at a Super Admin-configurable percentage (client-stated default 60%, explicitly expected to need tuning post-launch). (4) Store-sale payment collection happens entirely outside the application — the app only records the transaction, never a payment mode, for this specific kind of sale.
- No code was written — this was a documentation-only pass per the standing "resolved query ≠ permission to start coding" instruction. Written into `Docs/DOMAIN_LOGIC.md` §16.5 (new — Inventory Management), §16.6 (new — Repurchase limit), §16.7 (new — Item Buyback), §17.3 (rewritten from PENDING to RESOLVED), §17.4 (new — Payment Collection), and a new §21 resolved-log entry. `Docs/DATABASE_SCHEMA.md` Open Items noted the resolution too, explicitly flagging that no `store_inventory_items`/`store_buybacks` schema has been designed yet — that's T-014's own pre-coding pass, not part of this resolution.
- Evidence: `Docs/DOMAIN_LOGIC.md` §16.5/§16.6/§16.7/§17.3/§17.4/§21; `Docs/DATABASE_SCHEMA.md` Open Items; `Docs/TASKS.md` T-021 moved to Completed.
- Next: **T-014 (Store module) is now unblocked** — depends on T-004, T-008 (both built), but still needs its own pre-coding pass (schema, `ARCHITECTURE.md` notes, `TEST.md` worked scenarios) before any implementation code, per the standing pre-coding discipline. **T-019 (Wire scheduled jobs)** was already unblocked (depends only on T-010/T-011/T-013, all built) and needs no further prep. Ask the user which to start.
- Blocked by: none technically; explicit user go-ahead still required before starting either task, per standing instruction.

### 13-09-2026 — T-013: Daily Dummy Entry engine + leader assignment

- Done: Seeded a single fixed "Company" root Member (`CompanyRootMemberSeeder`) and built `GenerateDailyDummyEntries` (reads the enable toggle/daily count, places each entry via the existing occupied-side-traversal `BinaryPlacementResolver::resolve()` off that root's Right leg, gives each a real sequential Customer ID and `sponsor_id`=root) and `AssignDummyEntryToLeader` (converts an unassigned dummy's same row/id/Customer ID/tree position into a real leader — creates their first `users` row, password seeded to the Customer ID matching T-003's precedent, no plan/payment required).
- Before writing any code, asked the user the one genuinely blocking question this task had: §14.1's "always on the right side" placement never states an anchor point, and nothing in the schema had a root/company Member to anchor to. Confirmed: a single fixed seeded root, reused every day via the same occupied-side traversal real registration already uses — which naturally continues beneath whichever node is currently the bottom of the chain (still-unassigned dummy or already-assigned leader) with no extra "most recently assigned" bookkeeping needed.
- **While doing the pre-coding pass, found and fixed a real financial-integrity gap in already-completed T-007/T-011 code** — not a new business-rule question, a compliance bug: `CreatePairEntries` and `EvaluateBoosterQualification` both walk the Binary Position ancestor chain, which a dummy entry can legitimately sit inside, and neither had ever excluded an unassigned dummy from becoming a paid beneficiary, despite §14.2 point 5 being unambiguous that dummies "do not participate in compensation." Both Actions now skip such an ancestor (`is_company_dummy=true && dummy_status !== 'assigned'`) while still correctly counting it structurally for whichever real ancestor sits further up the same chain — the same guard `GenerateDrawGroups` (T-010) already used for the identical purpose. Recorded as `ARCHITECTURE.md`'s new Compensation Engine rule 5 for any future Action that walks this same chain.
- Evidence: `php artisan test` — 135/135 passing (6 new tests in `tests/Feature/DummyEntries/DummyEntryTest.php` reproducing `Docs/TEST.md` scenario 14's exact worked setup, plus 2 new regression tests added to the _existing_ `PairRewardTest.php`/`BoosterTest.php` files proving the dummy-exclusion fix — see `Docs/TASKS.md` T-013 entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — the Assignment Pool/Status view is Super Admin page territory, T-016/T-017's job). `Docs/TEST.md` scenario 14 (new) written before the implementation code. `Docs/ARCHITECTURE.md`/`Docs/DATABASE_SCHEMA.md` updated to match.
- Next: **T-014 (Store module) remains blocked** on the one still-open client-confirmation item (store inventory/repurchase/returns/POS details). T-015/T-016/T-017/T-018 are all transitively blocked by it too. T-019 (Wire scheduled jobs) is the next actually-unblocked task — it depends only on T-010/T-011/T-013, all now built. Awaiting user go-ahead before starting.
- Blocked by: none technically for T-019; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-012: Profile pending-fields (one-time) + Change Request workflow

- Done: Built `SubmitPendingProfileFields` (one-time submission of PAN/Aadhaar/Photo/Address plus Bank Account Details + Passbook/Cheque — folded into creating the member's one `member_bank_details` row rather than separate `members` columns, since none existed there anyway; locks via `pending_fields_submitted_at`), `VerifyMemberBankDetail` (Super Admin marks a bank detail verified — closes a precondition `SubmitPayoutRequest` (T-009) already required but nothing built), `SubmitProfileChangeRequest` (snapshots the old value, one `pending` request per field at a time), and `Approve/RejectProfileChangeRequest` (approval updates the field — or, for `bank_details`, updates the existing row in place and resets `verified_at` to null, requiring re-verification — both send `Notifications/ProfileChangeRequestReviewed`, the project's first notification).
- Before writing any code, did the required pre-coding pass and found a real architecture tension between two already-built pieces of schema: `members.passbook_or_cheque_path` (T-002) and `member_bank_details.proof_document_path` (T-009) both described the same passbook/cancelled-cheque proof image. Resolved by dropping the redundant `members` column (new migration) and treating `member_bank_details` as the single source of truth — no client ambiguity here, just two prior tasks' schema never having been reconciled against each other until this task needed both. 5 further decisions were similarly resolved from already-established text/principles without needing to ask the user this time (unlike T-010/T-011): building the bank-detail verification action now rather than leaving it dangling further; resetting `verified_at` on an approved bank-details change; one in-flight change request per field; PAN/Aadhaar format validation (objective government formats, not invented business rules); and building the notification via the database channel only, since no notifications infrastructure existed anywhere in the project before this task.
- Evidence: `php artisan test` — 127/127 passing (9 new tests in `tests/Feature/Profile/ProfileChangeRequestTest.php`, reproducing `Docs/TEST.md` scenario 13's exact worked setup — see `Docs/TASKS.md` T-012 entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (2 real `json_encode()`-can-return-`false` type gaps fixed properly, not cast away); `npm run build` clean (no UI surface this task — Complete Profile/Change Request are M03/M04, T-015's job). `Docs/TEST.md` scenario 13 (new) written before the implementation code. `Docs/ARCHITECTURE.md`/`Docs/DATABASE_SCHEMA.md` updated — the latter also retroactively marks T-011's already-completed Booster duration change as `RESOLVED` (it had been left as a stale "PENDING" note).
- Next: T-013 (Daily Dummy Entry engine + leader assignment) is next in backlog order — depends on T-004 (built). Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-011: Income Booster (qualification + 6-month schedule, concurrent multi-level)

- Done: Built `EvaluateBoosterQualification` (fires only off `registration` payments — Booster's inputs are structural, unlike Level Income/Pair Reward which also react to EMI installments; walks the new member's full Binary Position ancestor chain via `BinaryPlacementResolver::ancestorsWithSide()` and evaluates every level each ancestor newly qualifies for, not only the highest — matching §9.1's concurrency rule), `Services/BinaryTeamSizeCounter` (a single recursive SQL CTE per ancestor returning both legs' live descendant counts — §9's own implementation note called for a live recompute rather than T-007's consumable-ledger approach), and `Jobs/ProcessBoosterPayouts` (credits due `pending` schedule rows, idempotent via a lock + status re-check). A member can hold concurrent schedules across all 3 levels once separately qualified for each; qualification is a one-time gate per level (the 6-month schedule pays out regardless of a later drop below threshold).
- Before writing any code, did the required pre-coding pass and found one genuine business-rule gap: §9's table lists a "Team size" total alongside a separate "Team split (Left–Right)" column, but §9.1 never states whether the split is independently enforced or just illustrative of an even split. Asked the user directly — confirmed each side must independently reach its own half of the threshold (a 490L/10R team, 500 total, does not qualify for Level 1 despite the combined total being met). Also re-checked the standing §31.3 Financial Integrity artifact per T-007's/T-008's note — confirmed still just a docx mistake, no new conflict with this task's design. A genuine scalability gap was also caught by testing against the _real_ thresholds (not scaled-down fixtures): `ancestorsWithSide()`'s 500-level default depth trip-wire can be legitimately exceeded by occupied-side traversal for a 3,000-member Level-3 team, so `EvaluateBoosterQualification` calls it with an explicit 10,000-level override.
- Evidence: `php artisan test` — 118/118 passing (5 new tests in `tests/Feature/Compensation/BoosterTest.php`, reproducing `Docs/TEST.md` scenario 3's exact worked numbers — see `Docs/TASKS.md` T-011 entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — Booster's member-facing page is T-015's job). `Docs/TEST.md` scenario 3 extended with the split-independence regression case. A new migration adds a `(member_id, level_no)` unique constraint to `booster_qualifications` (a real pre-existing schema gap — only `booster_payout_schedules` had a uniqueness guard before this). `Docs/ARCHITECTURE.md` Compensation Actions/Services/Events lists and the Booster Payout Processor job-table row updated to match what was actually built.
- Next: T-012 (Profile pending-fields one-time entry + Change Request workflow) is next in backlog order — depends on T-003 (built). Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-010: Monthly Draw (grouping, execution, upline benefit)

- Done: Built `GenerateDrawGroups` (forms as many full groups as the eligible/ungrouped backlog allows per run, ordered by `members.id`, never a partial group) and `ExecuteMonthlyDraw` (idempotent per `(draw_group_id, cycle_month_no)`, requires that month's `draw_group_month_configs` row to already exist or skips without inventing a prize, secure `random_int()` winner selection with an auditable `rng_proof`, upline benefit to the winner's direct Sponsor at ≥10 Direct Members, marks a group `completed` at its 20th executed month) per `DOMAIN_LOGIC.md` §8. Also built `Events/DrawResultPublished` (`ShouldBroadcast`) so the real-time push DOMAIN_LOGIC.md §8.4 point 6 requires is code-complete — it safely logs under today's `BROADCAST_CONNECTION=log` default rather than erroring, ready the moment a real driver (Reverb, recommended) is installed.
- Before writing any code, did the required pre-coding pass and found one genuine business-rule gap: §8.1 never says what happens to leftover eligible members below a full batch at a grouping cutoff. Asked the user directly rather than guessing — confirmed leftovers roll over and wait for a future cutoff to complete the batch, never forming an undersized group. 6 further points were resolved from the existing text without needing to ask (all recorded in `DOMAIN_LOGIC.md` §21): ordering by `members.id` instead of string-sorting Customer IDs (avoids a real "GWL10" < "GWL2" bug); once-ever grouping semantics; unassigned-dummy exclusion (already resolved by §14, not a new rule); no live re-check of a grouped member's `active` status at execution time (the roster is a frozen snapshot per §8.2); requiring a pre-configured prize before executing a month rather than inventing one; and using the schema's forced-`executed` status directly, leaving `scheduled`/`reconciled` reserved for a future admin-correction workflow outside this task's scope.
- Evidence: `php artisan test` — 113/113 passing (7 new tests in `tests/Feature/Draw/DrawTest.php`, reproducing `Docs/TEST.md` scenario 6's exact worked setup — see `Docs/TASKS.md` T-010 entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (one real fix applied via the existing documented Larastan/Carbon tooling-gap workaround, plus an `array_chunk` int-narrowing fix — both genuine static-analysis catches, not suppressions); `npm run build` clean (no UI surface this task — the Draw Page/slot-machine animation are T-015/T-017's job, same scope boundary as T-006–T-009). `Docs/TEST.md` scenario 6 expanded from an upline-benefit-only example into the full grouping→execution→idempotency→cycle-completion→upline-benefit flow with worked numbers, written before the implementation code. `Docs/ARCHITECTURE.md` Draw Actions list and broadcast-driver note updated to match what was actually built.
- Next: T-011 (Income Booster) is next in backlog order — depends on T-004, T-008 (both built). Re-check `DOMAIN_LOGIC.md` §21's note on the §31.3 Financial Integrity copy-paste artifact before writing any Booster code, per the existing standing note on that task. Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-009: Payout request + Super Admin processing

- Done: Built the full Payout module per `DOMAIN_LOGIC.md` §11: `SubmitPayoutRequest` (member-side — validates the configured minimum and available balance, places a wallet hold immediately at submission via T-008's `WalletLedgerService::hold()`), `ProcessPayoutRequest` (Super Admin success path — computes TDS/fee snapshots, creates an immutable `payout_transactions` row, confirms the hold, supports bulk/batch via a shared `batchReference` string with each request staying its own atomic transaction), `FailPayoutRequest` (records an audit transaction row but releases rather than confirms the hold — §11.2 point 7's "never create a permanent debit"), and `RejectPayoutRequest`/`CancelPayoutRequest` (release the hold, zero transaction rows, only valid while `status=pending`). New models: `MemberBankDetail`, `PayoutRequest`, `PayoutTransaction`. No new migration — T-002's schema already had every needed column.
- Before writing any code, did the required pre-coding pass and made 4 real architecture decisions (not guesses) recorded in `DOMAIN_LOGIC.md` §21: (1) hold-at-submission rather than hold-at-review, so `availableBalance()` stays correct across multiple pending requests; (2) `payout_requests.status`'s `approved`/`processing` values are reserved for a future async provider integration and unused by today's manual-method flow (pending transitions straight to processed/failed), matching §11.2 point 2's "on approval, create a payout record" read literally; (3) bulk/batch is a parameter on `ProcessPayoutRequest`, not a separate orchestrator Action; (4) Reject and Cancel stay two distinct Actions/statuses. Also flagged (not guessed) one genuine spec gap: §11.1's Withdrawal Processing Fee shape (flat ₹ vs. percentage) is undefined in the source spec — implemented as a percentage matching TDS, default 0% (inert either way), added to §21's "Still open" table pending real client confirmation.
- Evidence: `php artisan test` — 106/106 passing (10 new tests in `tests/Feature/Payout/PayoutTest.php`, reproducing `Docs/TEST.md` scenario 7's exact worked numbers — see `Docs/TASKS.md` T-009 entry for the full list); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — Payout's member-facing/Admin-facing pages are T-015/T-017's job). `Docs/TEST.md` scenario 7 expanded from a single TDS-only example into the full request→hold→processing→reject/cancel→bulk→failure flow with worked numbers, written before the implementation code per the project's pre-coding discipline. `Docs/ARCHITECTURE.md` Payout Actions list updated to match what was actually built.
- Next: T-010 (Monthly Draw) is next in backlog order — depends on T-004, T-008 (both built). Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — Live UI theme colors (chat-driven, not a TASKS.md item)

- Done: User asked live in chat which CSS setup is used (Tailwind CSS v4, confirmed via `package.json`), then asked for a more attractive color theme than the starter kit's default black-and-white one, plus a light/dark toggle, with colors as a single-source variable set. The light/dark/system toggle already existed and worked (`resources/js/hooks/use-appearance.tsx` + `resources/js/components/appearance-tabs.tsx`, standard Laravel React starter-kit mechanism — toggles the `.dark` class on `<html>`, persisted to `localStorage`+cookie) — nothing new was built there. Replaced the grayscale `oklch()` values in `resources/css/app.css`'s `:root`/`.dark` blocks with a gold/navy/teal palette (fits the "GoldWave" brand name): warm gold `--primary`, deep navy `--background`/`--foreground` in dark mode, teal `--accent`, plus matching `--chart-1..5` and `--sidebar-*` values for both modes. No structural change — same CSS variable names Tailwind's `@theme` block already maps to `--color-*` utilities, so the existing "change colors in one place" property was already true and still holds.
- Evidence: `npm run build` — Vite build succeeded, no errors. This is a chat-driven UI change (not a `Docs/TASKS.md` item), recorded here per the standing "record chat-driven UI changes in Docs/*.md the same session" instruction. No business logic, schema, or architecture touched.
- Next: user has not yet visually confirmed the new palette in the browser; still owes T-009 go-ahead separately per the per-task checkpoint pace.
- Blocked by: none.

### 13-09-2026 — T-008: Wallet/Ledger service

- Done: Completed `DOMAIN_LOGIC.md` §12/§11.1's Wallet/Ledger service. `WalletLedgerService::credit()` already existed (front-loaded in T-006); this task added `hold()` (reserves an amount against `members.wallet_hold_amount` without touching the spendable `wallet_balance`, creating a `debit`/`status=pending` ledger row), `confirmHold()` (finalizes that same row to `confirmed`, decrementing both the hold and the balance — idempotent), `releaseHold()` (cancels it to `reversed`, decrementing only the hold — idempotent, used when a payout request is rejected/cancelled/fails per §11.1), and `availableBalance()` (`wallet_balance` minus `wallet_hold_amount`, what a member could request a new payout against). No new migration was needed — `wallet_ledger_entries.status`/`processed_at` were already exactly the right columns for this lifecycle.
- Before writing any code, did the required pre-coding pass and found a real tension worth resolving deliberately rather than picking silently: `DATABASE_SCHEMA.md`'s general "financial tables are append-only, rows never updated" principle seemed to conflict with `wallet_ledger_entries` having its own transition-shaped `status`/`processed_at` columns at all. Resolved by reading the schema evidence rather than guessing: `payout_requests.hold_ledger_entry_id` (already in the T-002 schema) is a single FK expecting exactly one row per hold, which only makes sense if that one row's own status is meant to transition over its lifecycle (reserve → finalize or release) rather than a second row being created each time. This is the transaction's own declared lifecycle completing, not a correction of a mistake — §22's "reverse/correct through linked transactions only" governs the latter, a genuinely different concern. Recorded in `DOMAIN_LOGIC.md` §21 as an architecture decision, not a business-rule guess.
- Evidence: `php artisan test` 96/96 (Pest, SQLite) — 8 new tests covering hold/confirm/release in isolation and together (a confirmed hold can never later be released and vice versa — status-guarded both directions), idempotency of both `confirmHold()` and `releaseHold()`, multiple concurrent holds on the same member resolving independently, and the hold's ledger row correctly linking back to an arbitrary source model via the existing polymorphic `source`; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) 0 errors; `npm run types:check`/`build` clean (no UI surface this task — the Wallet member-facing page is T-015's job). `Docs/TASKS.md` (T-008 moved to Completed with full verification note), `Docs/DOMAIN_LOGIC.md` (§21, new resolved-decision row), `Docs/ARCHITECTURE.md` (Services list, Status line).
- Next: T-009 (Payout request + Super Admin processing) is next in backlog order — depends on T-008 (built). Its debit-side work should call `WalletLedgerService::hold()`/`confirmHold()`/`releaseHold()` rather than writing new debit logic; see `Docs/DOMAIN_LOGIC.md` §11 and `Docs/TEST.md` scenario 7 for the TDS-deduction worked fixtures. Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-007: Pair/Reward with incremental consumed-entry tracking

- Done: Implemented `DOMAIN_LOGIC.md` §7's Pair/Reward engine as two Actions. `CreatePairEntries` (new `Actions/Compensation`) fires once per member's "fully eligible joining" — immediately on a one-time plan's registration payment, or on whichever EMI installment first reaches the plan's `pair_qualification_emis` threshold (Plan A=6, B/C=2, D=1) — and fans that single event out into one `pair_entries` row per Binary Position ancestor via a new `BinaryPlacementResolver::ancestorsWithSide()` (unbounded depth, each paired with which of the ancestor's two legs the joining's subtree falls under). `EvaluatePairMilestones` (new) runs per beneficiary — intended monthly — and consumes the oldest unused entries needed for the next unachieved milestone (only if both the Left/Right counts and the Min. Direct Members setting are satisfied), pays the reward via `WalletLedgerService::credit()` (T-006), and keeps climbing further milestones in the same run while enough entries remain. A `CreatePairEntriesOnPaymentConfirmed` listener attaches the fan-out to the existing `PaymentConfirmed` event, dispatched independently alongside T-006's Level Income listener (both auto-discovered, no coupling between them).
- Before writing any code, the required pre-coding pass surfaced a foundational gap: `DOMAIN_LOGIC.md` §7 never stated whose account gets credited for a new eligible joining — unlike Level Income's explicit 12-level Sponsor-chain walk. Reasoning through the schema (`pair_entries`' one `member_id`/`side`/independent `status` per row) and the milestone table showed an immediate-parent-only reading would make milestone 2 onward (50L/50R and beyond) mathematically unreachable, since Binary Position placement is strictly two children per member. This was surfaced to the user directly rather than guessed, and confirmed: Pair/Reward is a **team-size** count — every ancestor in the member's entire Binary Position chain, unbounded depth, gets credited on whichever leg the joining's subtree falls under, mirroring Income Booster's (§9) full-subtree semantics but via individually consumable, non-reusable entries (since Booster's live headcount recompute can't support "never reuse a consumed entry"). Recorded as a new "Pair/Reward Beneficiary Chain Rule" in `DOMAIN_LOGIC.md` §7 and in §21's resolved-decisions log.
- Also found and fixed, while designing this task's scheduling story: `DOMAIN_LOGIC.md` §19's background-jobs table never listed a job for §7.3's explicitly-required month-end Pair/Reward evaluation — added a "Pair/Reward Monthly Evaluator" row to both §19 and `ARCHITECTURE.md`'s mirrored table, and built `Jobs/EvaluateMonthlyPairMilestones` (iteration logic only — actual scheduler wiring is T-019's job, same precedent T-005 set for `ProcessEmiDueStatuses`).
- `rule_values` gained `pair_milestones` (the 15 thresholds + per-milestone Min. Directs gate), `pair_value_per_entry` (₹50), and `pair_qualification_emis`. Deliberately did **not** store each milestone's reward amount as a separate fixed number — it's derived as `(left + right) × pair_value_per_entry`, verified against every row of the §7.1 table (e.g. milestone 1: (5+5)×50 = ₹500), matching §7.4's listing of "pair value" as its own configurable setting distinct from the milestone thresholds. A new migration adds a `(source_payment_id, member_id)` unique constraint to `pair_entries` for fan-out idempotency.
- Evidence: `php artisan test` 88/88 (Pest, SQLite) — 8 new tests covering the EMI-qualification threshold for both Plan A and Plan D, the new team-size fan-out regression across a 3-ancestor chain (each credited on the correct leg), fan-out idempotency, `Docs/TEST.md` scenario 2's exact incremental-consumption/carry-forward numbers across two monthly evaluations with the "never reuse a consumed entry" invariant, the Min. Direct Members gate leaving a satisfied-but-gated milestone entirely unconsumed until met, crossing two milestones in a single run, and a full real-HTTP end-to-end run proving the event wiring fires with no manual glue; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) 0 errors; `npm run types:check`/`build` clean (no UI surface this task). `Docs/TASKS.md` (T-007 moved to Completed with full verification note), `Docs/DOMAIN_LOGIC.md` (§7's new Beneficiary Chain Rule, §19's new job row, §21's new resolved-decision row), `Docs/DATABASE_SCHEMA.md` (migration list, Status line), `Docs/ARCHITECTURE.md` (Actions/Services/Listeners/Jobs lists).
- Next: T-008 (Wallet/Ledger service) is next in backlog order — its credit-side (`WalletLedgerService::credit()`) already exists, front-loaded in T-006; T-008's remaining scope is `debit()`/hold logic for Payout processing (§12), needed by T-009. Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-006: Level Income calculation service

- Done: Implemented `DOMAIN_LOGIC.md` §6's Level Income engine. `CalculateLevelIncome` (new `Actions/Compensation`) walks the payer's Sponsor/Direct chain via `SponsorChainResolver` (T-004) up to 12 levels, reads `level_income_rates` from `RuleVersionService`, and creates one `income_ledger_calculations` row per level — `paid` with the resolved beneficiary credited, or `skipped` (with a reason) if the chain doesn't reach that level or the resolved beneficiary isn't currently `active`. A new `CalculateLevelIncomeOnPaymentConfirmed` listener attaches this to the existing `PaymentConfirmed` event (Laravel 11+ auto-discovers listeners in `app/Listeners`, confirmed via `php artisan event:list` — no manual registration needed), so both registration payments and EMI installment payments trigger it identically, with no EMI-specific branching.
- Front-loaded a minimal `Services/WalletLedgerService::credit()` in this task even though "Wallet/Ledger service" is nominally T-008. ARCHITECTURE.md's Compensation Engine rule 3 requires every compensation Action to credit through this service rather than writing `wallet_ledger_entries`/`members.wallet_balance` directly, and T-008 is listed as depending on T-006 — building only the credit-side now (the same precedent already used for `SponsorChainResolver` in T-004 and `RuleVersionService` in T-003) means T-008 extends this service with `debit()`/hold logic for Payout, rather than reopening T-006's `CalculateLevelIncome` to retrofit the call. This was a deliberate architecture decision, recorded in `DOMAIN_LOGIC.md` §21 and `ARCHITECTURE.md`, not something silently improvised.
- Two schema/rule gaps surfaced during the required pre-coding pass, both resolved and recorded in `DOMAIN_LOGIC.md` §21 rather than guessed: (1) `income_ledger_calculations.beneficiary_member_id` was NOT NULL since T-002, but a `chain_too_short` skip (the chain doesn't reach that level at all) has no member to attach — relaxed to nullable via a new migration, which also adds a `(source_payment_id, level_no)` DB-level uniqueness guard per ARCHITECTURE.md's stated idempotency principle; (2) §6.1's undefined "upline inactive" skip condition is resolved as reusing the already-settled `members.status === 'active'` definition (the same one used for sponsor-inactive-at-registration), not a new rule requiring separate client confirmation. `level_income_rates` was seeded into `rule_values` as a flat level→percent map rather than the source spec's "4-8"/"9-12" range-key shorthand, so no range-parsing logic is needed at lookup time.
- Evidence: `php artisan test` 80/80 (Pest, SQLite) — 6 new tests reproducing `Docs/TEST.md` scenario 1's exact worked numbers on a real 12-deep chain (₹5,000 payment → ₹800 total across 12 rows, matching the table exactly), the short-chain and inactive-upline skip behaviors, wallet-ledger linkage via the polymorphic `source`, double-invocation idempotency, and a full real-HTTP end-to-end run (`/join` → cash approval → an EMI installment payment) proving the event wiring fires with no manual glue and that each installment triggers its own independent calculation (scenario 8); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) 0 errors; `npm run types:check`/`build` clean (no UI surface this task). `Docs/TASKS.md` (T-006 moved to Completed with full verification note), `Docs/DOMAIN_LOGIC.md` (§21, two new resolved-decision rows), `Docs/DATABASE_SCHEMA.md` (migration list, Status line), `Docs/ARCHITECTURE.md` (Actions/Services/Listeners lists, Status line).
- Next: T-007 (Pair/Reward with incremental consumed-entry tracking) is next in backlog order — depends on T-004/T-005 (both built); see `Docs/DOMAIN_LOGIC.md` §7 and `Docs/TEST.md` scenarios 2/8 for the worked fixtures (milestone consumption with carry-forward, EMI-count qualification threshold). Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-005: EMI schedule generation and Payment In (online + cash)

- Done: Implemented `DOMAIN_LOGIC.md` §5/§10's full `emi_installments` schedule generation and the recurring installment Payment In flow, the two things T-003 explicitly stopped short of. `GenerateEmiInstallments` (new) creates every installment row on activation-date-anniversary due dates and marks installment #1 already `paid`, linked to the registration payment itself (per user confirmation — the amount that funded activation for an EMI plan already equals one installment, so the member is never asked to pay it twice). `InitiateEmiInstallmentPayment`/`ConfirmEmiInstallmentPayment` (new) extend `ConfirmOnlinePayment`/`ApproveCashPayment` with an `emi_installment` branch alongside the existing `registration` one, so the same webhook/cash-approval infrastructure serves both. `Member\EmiController` + `member/emi.tsx` give the member a working (if minimal — full M06 UI polish is T-015's job) page to see their schedule and pay the next-due installment; the "next-due-only, no skip-ahead" ordering rule is enforced server-side in `InitiateEmiInstallmentPayment`, not just in the UI.
- Before writing any code, did the required pre-coding pass (`Docs/advice.md`'s discipline) and found 3 genuine business-rule gaps no source spec or existing doc covered — surfaced to the user directly rather than guessed, all now recorded in `DOMAIN_LOGIC.md` §5 items 7-8/§21 and `Docs/TEST.md` scenario 12: (1) installment due-date cadence = activation-date anniversary (installment 1 due on activation date, each later one the same day-of-month a month on), with no grace period before a `due` installment becomes `overdue`; (2) a member may only pay their schedule's single earliest-unpaid installment — no advance/skip-ahead payment; (3) the registration payment for an EMI plan **is** installment #1, not a separate charge. Also resolved, as a pure architecture decision rather than a business-rule question: the Draw-eligibility EMI thresholds (`DOMAIN_LOGIC.md` §8.7), which `DATABASE_SCHEMA.md` had flagged as having "no schema home," need no new table/column — they'll live in the existing `rule_values` store exactly like the Pair/Reward EMI thresholds already do, and completed-installment counts are computed on demand rather than via a denormalized counter (matches the project's "status columns are the only lifecycle mechanism" decision).
- `Jobs/ProcessEmiDueStatuses` (new) holds the `upcoming`→`due`→`overdue` transition logic itself, but wiring it onto Laravel's scheduler is explicitly left to T-019 ("Wire scheduled jobs...", which also covers the other §19 jobs) — until then, installment 1 of every schedule is generated already correctly `due`/`paid`, so the core pay flow doesn't depend on this job running.
- Discovered and fixed along the way: (1) a real Larastan/Laravel-13 tooling gap — Larastan 3.11 doesn't reliably infer a `datetime`/`date`-cast attribute as `Carbon` on this Laravel version, even via the modern `casts()` method style; worked around with `Carbon::parse(...)` rather than a baseline entry, documented in `ARCHITECTURE.md` for future tasks to expect; (2) a small pre-existing gap from T-003 — `->with('status', ...)` flash messages (already used by the cash-approval controller) were never actually surfaced to the frontend, since `HandleInertiaRequests` never shared a `flash` prop; fixed by adding `flash.status` to its shared props; (3) while manually testing against the real Postgres dev DB, found `customer_id_counters.next_value` had drifted out of sync with reality (still `1` even though members up to `GWL19` already existed, because T-004's demo network was seeded by creating `Member` rows directly rather than through the real registration flow) — this is dev-data hygiene, not a code defect, since `CustomerIdGenerator` itself is correct for real registrations; fixed by advancing the counter to match.
- Evidence: `php artisan test` 74/74 (Pest, SQLite) — 7 new tests covering schedule generation numbers/dates, installment #1 auto-paid linkage, re-activation idempotency, next-due-only ordering rejection, cash and online Payment In flows (including webhook idempotency), and the due-status job's transitions; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) 0 errors; `npm run types:check`/`check`/`build` clean; manual end-to-end test via `php artisan serve` against the real Postgres dev DB, driven by actual authenticated HTTP requests (login, page fetch, pay endpoint, cash approval) rather than only Pest's sqlite suite — confirmed the schedule, the pay flow, and the super-admin cash-approval queue all work correctly on Postgres. `Docs/TASKS.md` (T-005 moved to Completed with full verification note), `Docs/DOMAIN_LOGIC.md` (§5 items 7-8, §19, §21), `Docs/DATABASE_SCHEMA.md` (Open Items — Draw-eligibility schema-home resolved), `Docs/TEST.md` (scenario 12, new), `Docs/ARCHITECTURE.md` (Actions/Jobs lists, Larastan/Carbon tooling note), `Docs/INSTRUCTIONS.md` (M06 — backend + minimal page shipped, full UI still T-015).
- Next: T-006 (Level Income calculation service) is next in backlog order — depends on T-004's `SponsorChainResolver` (already built) and T-005's confirmed EMI installment payments as the "eligible payment event" base (`DOMAIN_LOGIC.md` §6, `Docs/TEST.md` scenario 1). Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 13-09-2026 — T-004: Sponsor/Direct chain and Binary Position views, with live UI iteration

- Done: Implemented `DOMAIN_LOGIC.md` §4.1/§4.2's Directs View and Tree View. `SponsorChainResolver` (new — ARCHITECTURE.md had anticipated it for T-006's Level Income, introduced here since it's T-004's own "Sponsor/Direct chain" scope) and `BinaryPlacementResolver::isSelfOrDescendant` (added to the existing T-003 service) each provide an unbounded ancestor-walk used by a new `MemberPolicy` (`viewDirects`/`viewTree`) to authorize "a member may view their own downline, never outside it; Super Admin may view anyone." `Member\DirectsController`/`Member\TreeController` (the `Http/Controllers/Member/` folder ARCHITECTURE.md already named) each serve both roles from one controller, gated by the Policy rather than duplicated routes. Frontend: `member/directs` and `member/tree` pages, both rendered as tree diagrams.
- The user reviewed the first working version live and asked for several rounds of UI iteration in the same session, each implemented and documented immediately (per the explicit discipline the user asked to have recorded in `AGENTS.md`'s Documentation Contract during this task — see `Docs/INSTRUCTIONS.md` M08/M09 and `Docs/DOMAIN_LOGIC.md` §4.1/§4.2 for exactly what was added): (1) proper parent→child connector lines (a horizontal trunk between siblings' centers meeting a vertical stub from the parent, generalized in Directs View to N siblings with row-wrapping for members with more directs than fit one row — the wrapping connects each additional row to the row above via a stub from that row's own center); (2) a Back button on both views (implemented via browser history rather than a computed "parent" link, since it correctly retraces whatever path — clicks or search — the viewer actually took); (3) a Customer-ID search box on both views, resolving only within the searching member's own downline (`DirectsController`/`TreeController::search`) — a Customer ID that exists but sits on a different, unrelated leg ("cross-leg") is rejected with the exact same generic message as an unknown Customer ID, so the search can never be used to probe whether a given ID exists outside the viewer's authorized scope.
- Discovered and fixed along the way, not new scope — a real correctness gap in T-003's already-shipped pages: they never matched `resources/js/app.tsx`'s Inertia layout-naming convention (pages under `auth/` get `AuthLayout`, everything else defaults to the authenticated `AppLayout` sidebar shell). `registration/register`, `registration/status`, and `payments/dev-simulate` — all public, pre-login pages — were silently getting wrapped in the authenticated sidebar shell; `auth/goldwave-login` and `auth/goldwave-password-reset` were double-wrapped (their own full-page Card nested inside `AuthLayout`'s own centered container). Fixed by adding `registration/`/`payments/` to `app.tsx`'s null-layout cases and removing the redundant wrapper from both `auth/goldwave-*` pages to match the existing `login.tsx`/`register.tsx` convention (`.layout = {title, description}`). This had shipped in T-003 without being caught because the manual smoke test only checked HTTP status codes, not actual rendered layout — worth remembering for future manual-QA passes.
- Evidence: `php artisan test` 67/67 (Pest, SQLite) — Sponsor/Direct-vs-Binary-Position scoping, recursive navigation, Super Admin bypass, and the search feature's downline-only + cross-leg-rejection boundary all covered; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) 0 errors; `npm run types:check`/`check`/`build` clean; manual UI walkthrough via `php artisan serve` with a seeded demo network (a member sponsored by the root but placed under a different member, confirming Sponsor/Direct and Binary Position visually diverge as expected) and a 7-direct demo confirming row-wrapping. `Docs/TASKS.md` (T-004 moved to Completed with full verification note), `Docs/INSTRUCTIONS.md` (M08/M09 updated for the connector-line layout, Back button, search), `Docs/DOMAIN_LOGIC.md` (§4.1/§4.2 gained the search authorization boundary as an explicit rule), `Docs/ARCHITECTURE.md` (Controllers/Services lists updated to mark what's now built), `AGENTS.md` (Documentation Contract made explicit about recording live-chat UI iteration, per the user's direct request).
- Next: T-005 (EMI schedule generation + Payment In) is next in backlog order — reuses `CalculateEmiRateBooking` (T-003) for the per-installment formula; its own scope is the full `emi_installments` schedule generation and recurring installment payment/confirmation, which T-003 explicitly stopped short of. Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 12-09-2026 — T-003: authentication and registration → payment activation flow

- Done: Implemented the full flow per `DOMAIN_LOGIC.md` §2.1–§2.2, §3–§3.0, `FLOWCHART.md` J1–J3: sponsor-code validation (rejects invalid codes and inactive sponsors identically, re-checked live every attempt); `BinaryPlacementResolver` (occupied-side traversal, DB-enforced via a new `members_placement_unique` constraint with retry-on-race); mandatory Current/Future Rate Booking selection for EMI plans A–D with the exact §3.0 formula (`CalculateEmiRateBooking`, reused later by T-005); registration payment via Online (a `PaymentGatewayContract` interface + dev-only `FakePaymentGateway`/dev-simulate page, since no real gateway vendor is chosen yet — ARCHITECTURE.md) or Cash (Super Admin approval queue); `ActivateMembershipOnPaymentConfirmed` (idempotent — sequential Customer ID via a locked counter row, initial password = Customer ID, one-time-plan product benefit at the then-current rate); and the full dual-method Member login (`OtpService` + `PaymentGatewayContract`-style `OtpChannelContract` — real email via Laravel Mail, logging SMS stub since no SMS vendor exists — Customer-ID+password, and an OTP-gated password-reset flow with no bypass). Frontend: registration, login, password-reset, payment-dev-simulate, and Super-Admin cash-payment-approval pages (Inertia/React, using `useForm` for the dynamic/conditional registration form). GoldWave's Member login lives at `/member/login`, deliberately not replacing the Fortify-scaffolded generic `/login`/`/register` (left untouched and functionally dead for now — a plausible future Admin/Super Admin login path, since the source spec never defines one).
- Two schema/infra issues were discovered and fixed in-task (not business-rule changes — surfaced here per the project's discipline, not silently worked around): `members.customer_id` was NOT NULL from T-002 even though it's only assigned at activation (relaxed to nullable); `phpunit.xml` had no `APP_TIMEZONE`, so the test suite silently ran under UTC instead of the project's decided `Asia/Kolkata`, which caused a real date-boundary test failure in the Current Rate Booking metal-rate lookup and will matter again for T-010's Monthly Draw (fixed — `whereDate()` used for the rate lookups regardless, and `APP_TIMEZONE=Asia/Kolkata` added to `phpunit.xml`). Also: `RuleVersionService` was changed mid-task to cache only the scalar active-rule-version id (not the Eloquent model) after a caching bug surfaced — caching a model risks a stale/incompatible shape surviving a future `RuleVersion` structure change.
- Scope boundary carried forward: `emi_installments` rows are not generated by this task (only the `emi_schedules` header row, needed to price the registration payment) — full EMI schedule generation is T-005's job, which will reuse `CalculateEmiRateBooking`.
- Evidence: `php artisan test` 53/53 (Pest, SQLite) including `Docs/TEST.md` scenarios 9/10/11's exact worked numbers; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) 0 errors; `npm run types:check` and `npm run build` clean; manual end-to-end smoke test via `php artisan serve` against the real Postgres dev DB (sponsor validation → cash registration → signed status page → tampered-signature 403). `Docs/TASKS.md` (T-003 moved to Completed with full verification note), `Docs/DATABASE_SCHEMA.md` (Open Items updated, T-003 migrations table added), `Docs/ARCHITECTURE.md` (Actions/Services/Controllers lists updated to match the as-built `app/` structure, including the Registration/Auth/Payments public-flow Controller grouping not anticipated in the original layout).
- Next: T-004 (Sponsor/Direct chain and Binary Position placement independently) is next in backlog order — `BinaryPlacementResolver` already exists from this task, so T-004 is really about the Directs View/Tree View pages and any further chain-listing logic. Awaiting user go-ahead before starting.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 12-09-2026 — Sponsor-inactive-at-registration resolved (documentation only)

- Done: User confirmed the last remaining open item from the original consolidation — if a sponsor's own account is not currently Active (for any reason: own pending payment, Super Admin suspension, anything else), their invite/sponsor code must be **blocked**, treated exactly like an invalid code. No new member can register under an inactive sponsor. The check is on live status at the moment of registration, not a cached/one-time flag — a sponsor who becomes Active again can sponsor new members from that point on.
- Evidence: `Docs/DOMAIN_LOGIC.md` §2.1 (rule added to registration step 1), §21 (moved from "Still open" to "Resolved via client/user confirmation"); `Docs/TEST.md` scenario 9 (added a Given/Then for the blocked case and the re-activation regression check); `Docs/TASKS.md` T-003, T-021.
- Next: only 1 documentation-side item remains genuinely open — store inventory/repurchase/returns/POS details (T-014, explicitly deferred to the client). Awaiting user go-ahead to start T-003.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 12-09-2026 — Login & Authentication flow decided from scratch (documentation only — source spec never defined this)

- Done: User asked how login works after registration. Checked both client docx files (v1 and v2) directly — confirmed neither ever specifies a login mechanism at all (only lists "Login" and "password/account recovery" as page names, with zero detail). This was a real gap, not something missed during consolidation. Asked the user directly (after an initial multiple-choice attempt was declined in favor of open conversation) and got: (1) login identifier = mobile or email, either works; (2) two login methods, both always available — OTP (via SMS or email) and Customer-ID + password; (3) login only works after membership activation, never before; (4) initial/default password = the member's own Customer ID value; password can only be set/reset by first authenticating via OTP (no bypass "forgot password" flow); (5) confirmed as a separate point: the Online/Cash payment-mode choice at registration works identically for EMI plans (A–D, first installment) and one-time plans (E/F) — same mechanism, not plan-type-restricted. User explicitly said this must be documented and followed as the standing rule for all future work, not just this task.
- Evidence: `Docs/DOMAIN_LOGIC.md` §2.1 (mandatory mobile+email, Online/Cash parity note), §2.2 (new — full login mechanism), §3.1 step 11 (cross-reference); `Docs/DATABASE_SCHEMA.md` Open Items (new pending schema change — `mobile` column, OTP mechanism); `Docs/SECURITY.md` Authentication row (flags Customer-ID-as-default-password as a known, user-accepted risk, mitigated by throttling + mandatory OTP-gated reset); `Docs/TEST.md` scenario 11 (new worked example — pre-activation block, dual login methods, OTP-gated reset); `Docs/TASKS.md` T-003 scope note.
- Next: no documentation-side open items remain except the 2 genuinely open ones (sponsor-inactive-at-registration, store inventory/repurchase/returns/POS details — neither blocks T-003). Awaiting user go-ahead to start T-003.
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 12-09-2026 — Financial Integrity §31.3 artifact confirmed as a docx mistake (documentation only)

- Done: User confirmed the flagged possible copy-paste artifact in the client's spec v2.0 §31.3 Financial Integrity list ("Booster Direct/team qualification Show 6-month schedule" appearing where "Store calculation snapshots..." should be) is indeed a mistake in the client's document, not an intentional change. No content change was needed (the original principle was already kept in §22) — added a standing note instead: whoever implements T-011 (Income Booster) should re-check that spot when writing the code, and if any real conflict between Booster rules and the §22 reproducibility principle turns up, stop and discuss rather than assuming this resolution still covers it.
- Evidence: `Docs/DOMAIN_LOGIC.md` §21 (moved from "Still open" to the resolved log, with the standing implementation-time check note); `Docs/TASKS.md` T-011 (same note added); `Docs/PROGRESS.md` Snapshot.
- Next: no documentation-side open items remain except the 2 genuinely open ones (sponsor-inactive-at-registration, store inventory/repurchase/returns/POS details). Awaiting user go-ahead to start any task (T-003 next in backlog order).
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 12-09-2026 — Plan D Pair/Reward completed-EMI count resolved (documentation only)

- Done: User confirmed the one remaining number left open from the docx v2 comparison (below) — Plan D (₹10,000×10 months) requires a default of **1** completed EMI for Pair/Reward eligibility, Super-Admin-configurable/dynamic like the other plans (not left unconfigured).
- Evidence: `Docs/DOMAIN_LOGIC.md` §5, §7.1, §7.3, §21 (moved from "Still open" to "Resolved via client/user confirmation"); `Docs/TEST.md` scenario 8 (added a Plan D worked example) and scenario 10 note; `Docs/TASKS.md` T-021; `Docs/PROGRESS.md` Snapshot.
- Next: only the flagged possible copy-paste artifact in spec v2.0 §31.3 Financial Integrity remains for user/client confirmation (§21). Otherwise all documentation is current — awaiting user go-ahead to start any task (T-003 next in backlog order).
- Blocked by: none technically; explicit user go-ahead still required before starting any implementation task, per standing instruction.

### 12-09-2026 — Compared `GoldWave_Claude_instructions_2.docx` (client spec v2.0) against v1; docs updated (documentation only)

- Done: User provided an updated client document, `GoldWave_Claude_instructions_2.docx`, and asked for a comparison against the original `GoldWave_Claude_instructions.docx` plus any resulting doc updates so work can resume. Extracted both `.docx` files to plain text (via `word/document.xml` — no `pandoc`/`python-docx` available in this environment, used a small Node script instead) and diffed them. Found 4 real content changes: (1) **new Plan D** — ₹10,000/month × 10 months, 10gm Gold Jewellery; (2) **new dual Rate Booking mechanism** for all EMI plans (A–D) — Current Rate Booking (rate locked at booking, EMI = (value÷installments) + 1% maintenance cost) vs. Future Rate Booking (fixed rupee commitment, jewellery delivered at the rate on the delivery date, no maintenance cost) — this also resolves the previously-open "Plan product entitlement" CLIENT CONFIRMATION REQUIRED item; (3) **new EMI-based Draw-eligibility rule** (§8.7), explicitly stated to be separate from Pair/Reward EMI qualification even though 3 of 4 plans share the same numbers; (4) **Income Booster duration changed 3→6 months** and the table restructured with Min. Directs, binary team size, and Left/Right split columns (Level 3's total in the source docx, "₹36,0,000", is a comma-placement typo for ₹3,60,000 — corrected in our docs). Also found and flagged (not silently resolved): the ₹10,000 plan's Pair/Reward completed-EMI count has no client-given default (Super-Admin-configurable, unseeded), and a likely copy-paste artifact in spec v2.0 §31.3 where a Financial Integrity bullet was overwritten by Booster-table text (kept our original principle, flagged for confirmation). Wrote all of this into `DOMAIN_LOGIC.md`, `DATABASE_SCHEMA.md`, `TEST.md`, and `TASKS.md` — see the Snapshot row above for the full file list. **No code, migrations, or seed data were changed** — this was a documentation-only pass, consistent with the standing "resolved query ≠ permission to code" instruction.
- Evidence: `Docs/DOMAIN_LOGIC.md` §3 (plan table A–F), §3.0 (new, rate-booking formulas), §5, §7.1/§7.3, §8.7 (new), §9 (booster table/duration), §21 (2 new resolved logs + 2 new open items); `Docs/DATABASE_SCHEMA.md` Open Items (2 marked RESOLVED, 2 new "PENDING SCHEMA CHANGE — NOT YET APPLIED" notes); `Docs/TEST.md` scenario 10 (new — rate-booking worked example), scenarios 3 and 5 (updated for 6-month booster/concurrency and store-profit co-application); `Docs/TASKS.md` T-003/T-005/T-010/T-011/T-021 scope notes.
- Next: get user go-ahead on which task to start (T-003 is next in backlog order); separately, get a decision on the two flagged items (Financial Integrity artifact confirmation; Plan D Pair/Reward EMI count).
- Blocked by: none technically — explicit user go-ahead required before starting any implementation task, per standing instruction.

### 12-09-2026 — 4 open client-confirmation items resolved (documentation only)

- Done: User answered all 4 items that were awaiting client reply in `Docs/DOMAIN_LOGIC.md` §21: (1) Store Profit Distribution vs. Purchase/Repurchase Upline Income — both apply together on a member's store purchase (scenario 2 of 3 store-sale scenarios: walk-in non-member sale, member purchase, new-joining-with-jewellery-delivery); (2) a Store Owner is also a full network Member with a wallet; (3) Pair/Reward milestone minimum Direct Members = flat default of 2 for all 15 milestones, Super-Admin-configurable; (4) Income Booster allows concurrent 3-month schedules across all 3 levels if separately qualified. No code was written — this was a documentation-only update per the pre-coding discipline (resolve in `DOMAIN_LOGIC.md`, not by guessing in code).
- Evidence: `Docs/DOMAIN_LOGIC.md` §2 (Store Owner role), §7.3 (min Direct Members), §9.1 (Booster concurrency), §15/§16.4 (co-application rule + 3 store-sale scenarios), §21 (moved from "Still open" table to a new "Resolved via client/user confirmation (12-09-2026)" log); `Docs/PROGRESS.md` Snapshot updated.
- **User instruction recorded for all future work:** resolving an open query is NOT permission to start implementation code. The user must be asked explicitly before starting code on any task, every time — even after the relevant blocking query is answered — because other parts of the flow are still being finalized. This applies going forward, not just to T-003.
- Next: T-021 still needs client answers on plan Silver/Gold entitlement and sponsor-inactive-at-registration (still open in §21). T-003 remains not started — ask the user before beginning it.
- Blocked by: none technically, but per the instruction above, explicit user go-ahead is required before starting T-003 or any other implementation task.

### 10-09-2026 — Pre-coding checklist (Docs/advice.md) completed

- Done: User provided `Docs/advice.md` (a pre-coding checklist/workflow they wanted followed — architect-before-coder, full schema up front, business rules with worked examples, phased implementation, per-task read→plan→implement→test→verify→fix→document loop). Audited it point-by-point against work already done (T-000/T-002 covered most of it) and built the 2 genuine gaps found: (1) `Docs/ARCHITECTURE.md` — new file: layering, folder/namespace structure, compensation-engine isolation rules (SponsorChainResolver/WalletLedgerService/RuleVersionService as the only paths certain data can flow through), background-job/event wiring mapped to `DOMAIN_LOGIC.md` §19, permission structure per role; (2) an ERD (2 Mermaid diagrams) added to `Docs/DATABASE_SCHEMA.md`, and 9 worked-example Given/When/Then acceptance scenarios (with hand-verified real numbers) added to `Docs/TEST.md`, covering Level Income, Pair/Reward consumption, Booster, Purchase/Repurchase Income, Store Profit Distribution, Draw upline benefit, Payout TDS, EMI pair-qualification threshold, and Binary Placement traversal.
- Evidence: `Docs/ARCHITECTURE.md` (new), `Docs/DATABASE_SCHEMA.md` (ERD added), `Docs/TEST.md` (acceptance scenarios added), `Docs/README.md` (router updated for the new file), `Docs/DOMAIN_LOGIC.md` §21 (2 new open items discovered while writing the examples: Pair milestone min-direct-member counts never specified; Booster concurrent-schedule behavior undefined — plus a clarifying note that the Purchase/Repurchase duplicate-beneficiary guard is structurally inert in a real sponsor-chain walk, implemented defensively anyway), `Docs/TASKS.md` (T-021's blocked-task list expanded; intro note now points every future task at ARCHITECTURE.md + the relevant TEST.md scenario before starting).
- Next: T-003 (auth/registration), once the user gives the go-ahead.
- Blocked by: none for T-003 — see the 4-item client-confirmation list in the Snapshot row.

### 10-09-2026 — T-002: full database schema

- Done: Designed and implemented the complete GoldWave schema in one pass (16 migrations, 30 domain tables) per the restructured plan below. Decided app timezone = `Asia/Kolkata` (`config/app.php`, `.env`), money columns = `decimal(14,2)` everywhere, no soft-deletes anywhere (status columns are the sole lifecycle mechanism — no entity in `DOMAIN_LOGIC.md` is ever deleted), and centralized all Super-Admin-configurable rates/thresholds/taxes into a versioned `rule_versions` + `rule_values` (JSON) key-value store instead of ~8 separate normalized rate tables.
- Evidence: `php artisan migrate --force` clean; `php artisan migrate:fresh --force` clean (full rebuild); `php artisan migrate:rollback --step=16 --force` clean then re-migrated (verifies every `down()`); `php artisan test` — 39/39 still passing; `psql \dt` confirms 30 GoldWave tables + 10 framework tables = 40 total. `Docs/DATABASE_SCHEMA.md` rewritten to describe the as-built schema (was a pre-code conceptual blueprint).
- Discovered while designing `store_profit_distributions`: the Store Owner's 2% share (`DOMAIN_LOGIC.md` §16.4) has no defined payment mechanism if a Store Owner isn't also a network Member (wallets only belong to Members, §12) — added as a new open item in `Docs/DOMAIN_LOGIC.md` §21, schema accommodates either answer via nullable `beneficiary_member_id`/`beneficiary_user_id`.
- Next: T-003 (authentication + registration → payment activation flow), once the user gives the go-ahead per their checkpoint pace.
- Blocked by: none for T-003. Two items await client reply (see Snapshot row) but only block T-014 (Store module).

### 10-09-2026 — Task-list restructure: full schema up front

- Done: User proposed designing the entire GoldWave database schema in one pass instead of module-by-module (their stated experience: piecemeal schema work causes repeated ALTER TABLE churn, especially painful for cross-cutting tables like Wallet Ledger which references many other entities). Agreed — GoldWave's business logic is already fully specified in `Docs/DOMAIN_LOGIC.md`/`Docs/DATABASE_SCHEMA.md` (unlike a typical project, there's no remaining discovery phase), so committing to a complete physical schema now is low-risk. Restructured `Docs/TASKS.md`: merged the old T-002 (core entities) and T-003 (compensation/store entities) into one consolidated **T-002 — full schema design + migrations**; renumbered every task after it down by one (old T-004→new T-003, … old T-022→new T-021). Updated all cross-references in `Docs/PROGRESS.md`, `Docs/DOMAIN_LOGIC.md` §21, and `Docs/TEST.md` to the new IDs.
- Evidence: `Docs/TASKS.md` Pending list now reads T-002…T-021; `Docs/DOMAIN_LOGIC.md` §21's Store-distribution open item now says "Do not build T-014" (was T-015); `Docs/TEST.md` Status line now says T-020 (was T-021).
- Next: Design and implement T-002 (full schema + migrations for every entity in `Docs/DATABASE_SCHEMA.md`), verify with a clean `migrate`, then stop for review before T-003 per the user's checkpoint pace.
- Blocked by: none for T-002 itself.

### 09-09-2026 — T-001: project scaffold

- Done: Installed the Laravel installer globally; ran `laravel new --react --database=pgsql --pest --npm --no-boost --no-interaction` into a temp folder and merged it into the repo root (no filename collisions with `Docs/`/`AGENTS.md`/the `.docx`). Enabled the `pdo_pgsql`/`pgsql` PHP extensions in WAMP's shared `php.ini` (DLLs existed but were commented out). User provisioned a dedicated local Postgres role+DB (`goldwave`/`goldwave`, not the `postgres` superuser) and confirmed login works; pointed `.env`/`.env.example` at it (real password only in the gitignored `.env`). Ran `git init` (branch `main`) — nothing committed yet, no commit was requested.
- Evidence: `php artisan migrate --force` → 5 starter-kit migrations applied cleanly. `npm run build` → Vite build succeeded (2313 modules). `php artisan test` → 39/39 passed. `php artisan serve` → HTTP 200 on `/` and `/login`. Docs updated to match: `AGENTS.md` (runtime/DB/commands rows), `Docs/TEST.md` (real command table), `Docs/DATABASE_SCHEMA.md` (primary key / timestamp conventions now decided by framework defaults; timezone still open), `Docs/SECURITY.md` (Fortify-based session auth noted), `Docs/TASKS.md` (T-001 moved to Completed).
- Next: T-002 (domain migrations), once the user gives the go-ahead per the per-task checkpoint pace they chose.
- Blocked by: none. Open items are listed in the Snapshot row above, not blockers for starting T-002 itself.

### 09-09-2026 — Documentation consolidation

- Done: Read `GoldWave_Claude_instructions.docx` in full (all 793 paragraphs + every table, ~1042 lines of extracted content). Rewrote `AGENTS.md` project card and all `Docs/*.md` files (except `Screenshots/README.md`, which was already fine) to hold the complete, de-duplicated business specification, so future sessions never need to re-open the `.docx`.
- Evidence: `Docs/DOMAIN_LOGIC.md` (all business rules, resolved conflicts, still-open items), `Docs/INSTRUCTIONS.md` (full page inventory + acceptance checklists), `Docs/DATABASE_SCHEMA.md` (logical blueprint), `Docs/SECURITY.md`, `Docs/PERFORMANCE_GUIDE.md`, `Docs/TEST.md`, `Docs/FLOWCHART.md` (10 journeys incl. Mermaid draw-cycle diagram), `Docs/TASKS.md` (T-001…T-022 backlog matching the client's own implementation order), `Docs/SEO_STRATEGY.md` (marked Not applicable), `Docs/README.md` (status table updated).
- Next: Begin T-001 (now done — see entry above).
- Blocked by: none for T-001. The 3 "still open" items in `Docs/DOMAIN_LOGIC.md` §21 need a real client answer before their dependent tasks.

## Entry Format

### 25-08-2026 — T-001

- Done: …
- Evidence: files changed; commands run and result
- Next: one concrete action
- Blocked by: none / exact decision or missing access
