# Task Tracker

Track only approved, actionable work. [TBD] must never appear as a task title.

## Rules

- Each task needs a stable ID, concise outcome, status, and dependencies when relevant.
- Status is exactly: Current, Pending, Blocked, or Completed.
- Before starting, check for duplicates and dependencies. On completion, move the item to Completed with date in DD-MM-YYYY format and a verification note.
- Do not create empty replacement tasks. Add a task only when real work is agreed or discovered.

## Entry Format

- [ ] **T-001 — Outcome** · Status: Pending · Depends on: none
- [x] **T-001 — Outcome** · Completed: 25-08-2026 · Verified: command or QA

## Current

_No active task._

## Pending

The backlog below follows the client-approved implementation order (`DOMAIN_LOGIC.md` §"31.1 Implementation order" in the original spec, now folded into this ordering). Start at T-001; do not skip ahead unless a later item is independently unblocked and agreed. **Before starting T-003 or any later task, read `Docs/ARCHITECTURE.md` (where the code for that task goes) and the relevant scenario(s) in `Docs/TEST.md`'s "Business-Rule Acceptance Scenarios" (the exact numbers that task's code must produce).**

## Blocked

_No blocked task._

## Completed

- [x] **T-000 — Consolidate `GoldWave_Claude_instructions.docx` into `Docs/` (DOMAIN_LOGIC, INSTRUCTIONS, DATABASE_SCHEMA, SECURITY, PERFORMANCE_GUIDE, TEST, FLOWCHART, TASKS, PROGRESS, SEO_STRATEGY, AGENTS.md, README)** · Completed: 09-09-2026 · Verified: manual review of all `.docx` paragraphs and tables against the new docs; no section left unmapped.
- [x] **T-001 — Scaffold Laravel + Inertia.js + React + PostgreSQL project skeleton** · Completed: 09-09-2026 · Verified: `laravel new --react --database=pgsql --pest --npm --no-boost` scaffolded (Laravel 13, PHP ^8.3, React 19 + TS + Tailwind v4 + Radix/shadcn, Fortify auth, Pest/Pint/Larastan); merged into repo root; enabled `pdo_pgsql`/`pgsql` PHP extensions (were present but disabled in WAMP's `php.ini`); created local `goldwave` Postgres role/DB (user-provisioned, not the `postgres` superuser); `.env`/`.env.example` pointed at it; `php artisan migrate --force` ran clean (5 starter-kit migrations); `npm run build` succeeded; `php artisan test` — 39/39 passed; `php artisan serve` returned HTTP 200 on `/` and `/login`. `git init` run (branch `main`) but nothing committed yet — no commit requested.
- [x] **T-002 — Design and implement the complete GoldWave database schema (all migrations, up front)** · Completed: 10-09-2026 · Verified: 16 migrations covering all entities in `Docs/DATABASE_SCHEMA.md` (30 GoldWave tables) — `php artisan migrate` clean, `php artisan migrate:fresh` clean (full rebuild from zero), `php artisan migrate:rollback --step=16` clean (down() methods verified) then re-migrated, `php artisan test` — 39/39 still passing. Decided during this task: app timezone = `Asia/Kolkata`, money columns = `decimal(14,2)`, no soft-deletes anywhere (status columns are the only lifecycle mechanism), and a versioned `rule_versions`/`rule_values` key-value store instead of one normalized table per configurable rate type. Discovered a new open item (Store Owner beneficiary identity — see `Docs/DOMAIN_LOGIC.md` §21) while designing `store_profit_distributions`; schema supports either resolution without a further migration. `Docs/DATABASE_SCHEMA.md` rewritten to document the as-built schema.
- [x] **T-003 — Implement authentication and registration → payment activation flow** · Completed: 12-09-2026 · Verified: `php artisan test` — 53/53 passing (Pest, incl. Docs/TEST.md scenarios 9/10/11's exact worked numbers — Plan A Current Rate Booking = ₹2,100/month, binary placement occupied-side traversal, sponsor-inactive rejection, OTP login via both mobile and email, Customer-ID+default-password login, OTP-gated password reset with no bypass, webhook idempotency, tampered-signature rejection); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run types:check` and `npm run build` clean; manual end-to-end smoke test against the real Postgres dev DB via `php artisan serve` (sponsor validation → cash registration → signed status page → tampered-signature 403) confirmed alongside the automated suite. 5 new migrations (mobile column, otp_codes, members.customer_id nullable + placement unique constraint, EMI rate-booking columns, customer_id_counters); Plans A–F seeded; Contracts (`PaymentGatewayContract`, `OtpChannelContract`) with dev-only `FakePaymentGateway`/logging `SmsOtpChannel`/real `EmailOtpChannel` swappable bindings since no payment/SMS vendor is chosen yet (ARCHITECTURE.md); `BinaryPlacementResolver`, `CustomerIdGenerator`, `RuleVersionService`, `OtpService` Services; full Registration/Payments/Auth Actions; Member-only dual login (OTP + Customer-ID/password) built alongside — not replacing — the untouched Fortify-scaffolded generic `/login`/`/register` (left as a plausible future Admin/Super Admin login path, since the source spec never defines one; see `Docs/PROGRESS.md`). **Scope boundary carried forward to T-005:** this task creates the `emi_schedules` header row (rate-booking selection + computed installment amount, needed to price the registration payment) but does **not** generate `emi_installments` rows — that is T-005's job. **Discovered during implementation (fixed here, documented, not deferred):** `members.customer_id` was NOT NULL in the T-002 schema even though DOMAIN_LOGIC.md §2.1/§3.1 assigns it only at activation — relaxed to nullable; `phpunit.xml` had no `APP_TIMEZONE`, silently testing under UTC instead of the project's decided `Asia/Kolkata` (DATABASE_SCHEMA.md) — added, which will also matter for T-010's Monthly Draw date-boundary logic.
- [x] **T-004 — Implement Sponsor/Direct chain and Binary Position placement independently** · Completed: 13-09-2026 · Verified: `php artisan test` — 67/67 passing (Pest — includes Directs View/Tree View scoping to Sponsor/Direct vs. Binary Position respectively, recursive navigation, Super Admin bypass, and the search feature's downline-only + cross-leg-rejection boundary); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run types:check`/`check`/`build` clean; manual UI walkthrough with a seeded demo network confirming Sponsor/Direct vs. Binary Position visually diverge as expected (a member sponsored by the root but placed under a different member appears in the root's Directs View but not directly under the root in Tree View). `SponsorChainResolver` (app/Services) — the ancestor-walk service ARCHITECTURE.md anticipated for T-006+, introduced here since it's this task's "Sponsor/Direct chain" scope even though no compensation Action consumes it yet; `BinaryPlacementResolver` gained the equivalent placement-chain check. `MemberPolicy` (`viewDirects`/`viewTree`) enforces "own downline only" for a plain member, bypassed for Super Admin. `Member/DirectsController` and `Member/TreeController` (both under the `Http/Controllers/Member/` folder ARCHITECTURE.md already anticipated) serve both roles from one controller each, gated by the Policy rather than separate routes. **Live UI iteration with the user, each round implemented and documented in the same pass** (see `Docs/INSTRUCTIONS.md` M08/M09, `Docs/DOMAIN_LOGIC.md` §4.1/§4.2 — per the explicit discipline the user asked to have recorded in `AGENTS.md`'s Documentation Contract during this task): (1) both views rendered as tree diagrams with parent→child connector lines (a horizontal trunk between siblings' centers, vertical stubs down to each card), Directs View generalizing the same technique to N siblings with row-wrapping when there are more directs than fit one row; (2) a Back button (browser history) on both views; (3) a Customer-ID search box on both views, resolving only within the searching member's own downline — a Customer ID that exists but sits outside it ("cross-leg") is rejected with the same generic message as an unknown one, never confirming/denying existence outside the viewer's authorized scope (new `DirectsController::search`/`TreeController::search` + matching routes, registered before the `{member?}` routes so the literal `search` path segment isn't swallowed by optional route-model binding). **Discovered and fixed along the way (not new scope, a correctness gap in T-003's already-shipped pages):** T-003's public/auth pages didn't follow the project's Inertia layout-naming convention (`resources/js/app.tsx`) — `registration/*`/`payments/dev-simulate` were defaulting to the authenticated `AppLayout` sidebar shell instead of no layout, and `auth/goldwave-login`/`auth/goldwave-password-reset` were double-wrapped (their own full-page Card inside `AuthLayout`'s own centered container); fixed by adding `registration/`/`payments/` to the layout switch's null-layout cases and removing the redundant wrapper from both `auth/goldwave-*` pages to match the existing `login.tsx`/`register.tsx` `.layout = {title, description}` convention.
- [x] **T-005 — Implement EMI schedule generation and Payment In (online + cash)** · Completed: 13-09-2026 · Verified: `php artisan test` — 74/74 passing (Pest — 7 new tests: full 20-row schedule generation with activation-date-anniversary due dates, installment #1 auto-paid via the registration payment, re-activation idempotency, next-due-only payment ordering rejection, cash Payment In confirm-and-advance, online Payment In webhook idempotency, ProcessEmiDueStatuses upcoming→due→overdue transitions with no grace period); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (surfaced and worked around a real Larastan/Laravel-13 tooling gap — see `Docs/ARCHITECTURE.md`'s "Larastan/Carbon tooling note"); `npm run types:check`/`check`/`build` clean; manual end-to-end smoke test against the real Postgres dev DB via `php artisan serve` — registered a new member (cash, Plan A, Current Rate Booking) through the real HTTP routes with a live authenticated session (not just Pest's sqlite suite), confirmed the schedule (20 × ₹2,100 = ₹42,000, correct 13th-of-month anniversary due dates), paid installment #2 via the real `/member/emi/{id}/pay` endpoint, approved it as Super Admin, and confirmed installment #3 correctly stayed `upcoming`. **Discovered and fixed along the way (dev-data hygiene, not a code defect):** this Postgres dev DB's `customer_id_counters.next_value` was still `1` even though members up to `GWL19` already existed (from T-004's demo network, seeded by directly creating `Member` rows rather than through the real registration flow) — the first real end-to-end registration through `CustomerIdGenerator` collided on `GWL01`. Fixed by advancing the counter to `20`; no code change, since `CustomerIdGenerator` itself was correct — real registrations always go through it. Three business-rule gaps found while doing the required pre-coding pass (architecture/schema/test-scenario review) were resolved via direct user confirmation rather than guessed, all recorded in `DOMAIN_LOGIC.md` §5 items 7-8/§21 and `Docs/TEST.md` scenario 12: (1) installment due-date cadence = activation-date anniversary, no grace period before `overdue`; (2) a member may only pay their single next-due installment, never skip ahead; (3) the registration payment itself **is** installment #1 (not a separate charge) — the member is never asked to pay it twice. Also resolved as a pure architecture decision (not a business-rule question): the Draw-eligibility EMI thresholds (§8.7) needed no new schema — they'll live in the existing `rule_values` store like the Pair/Reward EMI thresholds already do, and a member's completed-installment count is computed on demand rather than a denormalized counter (`Docs/DATABASE_SCHEMA.md`). New: `Actions/Registration/GenerateEmiInstallments`, `Actions/Payments/{ConfirmEmiInstallmentPayment,InitiateEmiInstallmentPayment}`, `Jobs/ProcessEmiDueStatuses` (transition logic only — wiring it onto the scheduler is explicitly left to T-019, alongside the other §19 jobs), `Http/Controllers/Member/EmiController` + `member/emi.tsx` (a working backend + minimal page — the full designed M06 UI is still T-015's job, see `Docs/INSTRUCTIONS.md`). `ConfirmOnlinePayment`/`ApproveCashPayment` gained an `emi_installment` branch alongside their existing `registration` one; `CashPaymentApprovalController` (already generic by `mode`/`cash_status`, not `type`) now also eager-loads and displays EMI-installment context. Also fixed a small pre-existing gap from T-003: `->with('status', ...)` flash messages (already used by the cash-approval controller) were never actually surfaced to the frontend — `HandleInertiaRequests` now shares a `flash.status` prop.
- [x] **T-006 — Implement Level Income calculation service** · Completed: 13-09-2026 · Verified: `php artisan test` — 80/80 passing (Pest — 6 new tests: Docs/TEST.md scenario 1's exact worked numbers reproduced on a real 12-deep Sponsor/Direct chain — ₹5,000 payment → L1 ₹250/5%, L2-3 ₹100/2% each, L4-8 ₹50/1% each, L9-12 ₹25/0.5% each, total ₹800; a chain shorter than 12 records `skipped`/`chain_too_short` rows for the unreachable levels instead of omitting them; an inactive upline beneficiary is skipped with `skip_reason=upline_inactive` while other levels are unaffected; wallet ledger entries created per paid level, correctly linked back to their `income_ledger_calculations` row via the polymorphic `source`; recalculating the same payment twice never duplicates rows or double-credits; a full end-to-end run through the real `/join` → cash-approval HTTP flow confirms `PaymentConfirmed` auto-fires Level Income with no manual wiring, and a later EMI installment payment triggers its own independent calculation per scenario 8); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run types:check`/`build` clean (no UI surface this task). New: `Actions/Compensation/CalculateLevelIncome`, `Listeners/CalculateLevelIncomeOnPaymentConfirmed` (auto-discovered — confirmed via `php artisan event:list`, no manual `Event::listen` needed on Laravel 13), `Models/IncomeLedgerCalculation`, `Models/WalletLedgerEntry`. **Front-loaded ahead of its nominal task** (documented in `ARCHITECTURE.md`/`DOMAIN_LOGIC.md` §21, same precedent as `SponsorChainResolver`/`RuleVersionService`): a minimal `Services/WalletLedgerService::credit()` — ARCHITECTURE.md's Compensation Engine rule 3 requires every compensation Action to credit through it, so building it here avoids T-008 requiring rework of this task's code; T-008 will add `debit()`/hold logic for Payout on top. **Two schema/business-rule gaps surfaced during the required pre-coding pass, both resolved and recorded in `DOMAIN_LOGIC.md` §21 rather than guessed:** (1) `income_ledger_calculations.beneficiary_member_id` was NOT NULL since T-002 even though a `chain_too_short` skip has no member to attach — relaxed to nullable via a new migration, which also adds a `(source_payment_id, level_no)` DB-level uniqueness guard per ARCHITECTURE.md's idempotency principle; (2) §6.1's undefined "upline inactive" skip condition is resolved as reusing the existing `members.status === 'active'` definition, not a new rule. `level_income_rates` seeded into `rule_values` as a flat level→percent map (`{"1":5,...,"12":0.5}`) rather than the source's "4-8"/"9-12" range-key shorthand, so the lookup needs no range parsing.
- [x] **T-007 — Implement Pair/Reward with incremental consumed-entry tracking** · Completed: 13-09-2026 · Verified: `php artisan test` — 88/88 passing (Pest — 8 new tests: `Docs/TEST.md` scenario 8's EMI-qualification threshold for both Plan A — zero `pair_entries` for installments 1-5, exactly one at installment 6, none added for installments 7+ — and Plan D — one row on the very first installment; a new team-size fan-out regression across a 3-ancestor Binary Position chain, each ancestor correctly credited on the side matching which of their two legs the joining's subtree falls under; fan-out idempotency; scenario 2's exact incremental-consumption/carry-forward numbers reproduced end-to-end across two monthly evaluations, including the "never reuse a consumed entry" invariant; a Min. Direct Members gate test — a milestone whose L/R counts are satisfied but whose directs gate isn't met is left entirely unconsumed until the gate is met, never wasting entries; a single run crossing two milestones at once per §7.2 point 6; a full end-to-end run through the real `/join` → cash-approval HTTP flow confirming `PaymentConfirmed` auto-fires pair-entry fan-out with no manual wiring); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run types:check`/`build` clean (no UI surface this task). New: `Actions/Compensation/{CreatePairEntries,EvaluatePairMilestones}`, `Listeners/CreatePairEntriesOnPaymentConfirmed` (auto-discovered, dispatched independently alongside T-006's Level Income listener off the same `PaymentConfirmed` event), `Jobs/EvaluateMonthlyPairMilestones` (iteration logic only — scheduler wiring left to T-019, same precedent as T-005's `ProcessEmiDueStatuses`), `Models/{PairEntry,PairRewardTransaction}`, `BinaryPlacementResolver::ancestorsWithSide()` (the unbounded Binary Position ancestor walk, paired with which leg each ancestor sees the joining's subtree on). **One foundational gap surfaced and resolved via direct user confirmation before writing any code** (`DOMAIN_LOGIC.md` §7's new "Pair/Reward Beneficiary Chain Rule", §21): §7 never stated whose account gets credited for a new eligible joining — unlike Level Income's explicit 12-level Sponsor-chain walk. Worked out from the schema and the milestone table that an immediate-parent-only reading would make milestone 2+ (50L/50R and beyond) mathematically unreachable (Binary Position is strictly two children per member), then confirmed with the user: pairing is a **team-size** count — every ancestor in the member's entire Binary Position chain, unbounded depth, accumulates on whichever leg the joining's subtree falls under, mirroring Income Booster's (§9) full-subtree semantics but via individually consumable, non-reusable entries rather than a live headcount recompute. Also found and fixed a real gap in `DOMAIN_LOGIC.md` §19's job table while designing this task's scheduling story: §7.3 explicitly requires month-end Pair/Reward evaluation but §19 never listed a job for it — added "Pair/Reward Monthly Evaluator" to both `DOMAIN_LOGIC.md` §19 and `ARCHITECTURE.md`'s mirrored table. `pair_milestones`/`pair_value_per_entry`/`pair_qualification_emis` seeded into `rule_values`, with each milestone's reward intentionally derived as `(left + right) × pair_value_per_entry` rather than stored as a separate fixed number (verified against every row of the §7.1 table, e.g. milestone 1: (5+5)×50 = ₹500) — matches §7.4's listing of "pair value" as its own separate configurable setting from the milestone thresholds. A new migration adds a `(source_payment_id, member_id)` unique constraint to `pair_entries` for fan-out idempotency, per ARCHITECTURE.md's stated idempotency principle.
- [x] **T-008 — Implement Wallet/Ledger service** · Completed: 13-09-2026 · Verified: `php artisan test` — 96/96 passing (Pest — 8 new tests: `hold()` reserves an amount against `wallet_hold_amount` without touching spendable `wallet_balance`; `confirmHold()` finalizes it into an actual debit (decrements both) and is idempotent on a second call; `releaseHold()` cancels a hold with no debit at all and is likewise idempotent; a confirmed hold can never later be released and a released hold can never later be confirmed — status-guarded both ways; multiple concurrent holds on the same member reduce `availableBalance()` independently and correctly as each resolves; a hold's `wallet_ledger_entries` row correctly links back to an arbitrary source model via the existing polymorphic `source`); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run types:check`/`build` clean (no UI surface this task — Wallet's member-facing page is T-015's job). New: `WalletLedgerService::{hold,confirmHold,releaseHold,availableBalance}`, completing the class T-006 front-loaded with only `credit()`. No new migration needed — `wallet_ledger_entries.status`/`processed_at` (already in the T-002 schema) were exactly the columns this design needed. **One schema/docs tension resolved via architecture decision, not a business-rule guess, recorded in `DOMAIN_LOGIC.md` §21:** `DATABASE_SCHEMA.md`'s general "financial tables are append-only, rows never updated" rule appears to conflict with `wallet_ledger_entries` having transition-shaped `status`/`processed_at` columns at all — resolved by reading `payout_requests.hold_ledger_entry_id` (a single FK expecting exactly one row per hold) as confirming the intended design: a hold's own pending→confirmed/reversed transition updates that same row, since this is the transaction's own declared lifecycle completing, not a correction of a mistake (which is what §22's "reverse/correct through linked transactions only" actually governs). T-009 (Payout request/processing) will call these hold/confirm/release primitives rather than building its own debit logic — no rework anticipated.
- [x] **T-009 — Implement Payout request + Super Admin processing (Cheque/GPay/UPI/Bank Transfer/bulk)** · Completed: 13-09-2026 · Verified: `php artisan test` — 106/106 passing (Pest — 10 new tests reproducing `Docs/TEST.md` scenario 7's exact worked numbers: submitting a ₹10,000 request places a matching wallet hold without touching spendable balance; below-minimum and beyond-available-balance requests are rejected with no row and no hold created; processing at TDS=5%/fee=1% yields `tds_amount=500`, `processing_fee=100`, net transfer ₹9,400, and confirms the hold; at the real 0%/0% defaults the net transfer equals the full amount exactly; reject and cancel both release the hold with zero `payout_transactions` rows, differing only in resulting status; a terminal (processed) request can never be rejected/cancelled/reprocessed; bulk processing shares one `batch_reference` across 2 independently-created `payout_transactions` rows, and a request whose hold was already released before the batch runs is skipped without blocking the other 2; a failed payout still creates an audit transaction row (`status=failed`) but releases the hold instead of confirming it); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — Payout's member-facing/Admin-facing pages are T-015/T-017's job). New: `Models/{MemberBankDetail,PayoutRequest,PayoutTransaction}`; `Actions/Payout/{SubmitPayoutRequest,ProcessPayoutRequest,FailPayoutRequest,RejectPayoutRequest,CancelPayoutRequest}`; 3 new `rule_values` keys (`payout_min_amount=500`, `payout_tds_percent=0`, `payout_processing_fee_percent=0`). No new migration needed — T-002's `payout_requests`/`payout_transactions`/`member_bank_details` tables already had every column this design needed. **4 pre-coding architecture decisions made before writing code (all recorded in `DOMAIN_LOGIC.md` §21, none guessed):** (1) the wallet hold is placed at request _submission_, not deferred to admin review, so `availableBalance()` correctly reflects amounts already tied up across multiple pending requests; (2) `payout_requests.status`'s `approved`/`processing` values are reserved for a future async `in_app_provider` integration and unused by T-009's manual-method (cheque/GPay/bank-transfer) flow, which transitions `pending` directly to a terminal `processed`/`failed` state, matching §11.2 point 2's "on approval, create a payout record" reading literally; (3) bulk/batch processing is a shared `batchReference` string parameter on `ProcessPayoutRequest`, not a separate orchestrator Action — each request stays its own atomic transaction so one failure never blocks or rolls back the others in the same batch; (4) Reject and Cancel are two distinct Actions/statuses (both release the hold, create no transaction row) rather than merged, since the schema already reserves both enum values and the distinction costs nothing. **One genuine spec gap flagged rather than guessed, added to `DOMAIN_LOGIC.md` §21's "Still open" table:** §11.1's Withdrawal Processing Fee is explicitly "not currently defined" — implemented as a Super-Admin-configurable percentage (matching TDS's mechanism) with a default of 0% that keeps it inert until the client confirms whether they actually want a flat-₹ fee instead.
- [x] **T-010 — Implement Monthly Draw (grouping, execution, upline benefit)** · Completed: 13-09-2026 · Verified: `php artisan test` — 113/113 passing (Pest — 7 new tests reproducing `Docs/TEST.md` scenario 6's exact worked setup: grouping forms exactly one full 200-member group from a 250-member eligible backlog and leaves the 50 leftover ungrouped, is idempotent on an immediate rerun, and forms exactly one more full group once 150 more members arrive — 50 old + 150 new = 200, not split or reshuffled; a below-EMI-threshold Plan A member and an unassigned company dummy are both excluded from a group's roster; execution selects a winner, removes them from the pool, and is idempotent per `(draw_group_id, cycle_month_no)` — a retried same-month run creates no duplicate, and month 2 never re-selects month 1's winner; a group whose current month has no configured `draw_group_month_configs` row is skipped with no invented prize, without blocking a separately-configured group in the same run; a group's `status` becomes `completed` exactly at its 20th executed month and never attempts a 21st; the upline draw benefit fires at exactly 10 Direct Members and correctly does not at 9, with the winner still receiving their own prize either way); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (one real Larastan/Carbon tooling-gap fix applied per the existing `ARCHITECTURE.md` note, plus an `array_chunk` int-narrowing fix); `npm run build` clean. New: `Models/{DrawGroup,DrawGroupMember,DrawGroupMonthConfig,DrawExecution}`; `Actions/Draw/{GenerateDrawGroups,ExecuteMonthlyDraw}`; `Events/DrawResultPublished` (`ShouldBroadcast`, safely logs under today's `BROADCAST_CONNECTION=log` default rather than erroring — code-complete and ready the moment a real broadcast driver is installed); 2 new `rule_values` keys (`draw_group_size=200`, `draw_eligibility_emis` — a key deliberately separate from `pair_qualification_emis` per §8.7's explicit instruction not to merge the two checks even where Plans A/B/C's numbers happen to match). No new migration — T-002's `draw_groups`/`draw_group_members`/`draw_group_month_configs`/`draw_executions` tables already had every column this design needed. **One genuine business-rule gap resolved via direct user confirmation before writing any code** (`DOMAIN_LOGIC.md` §21): §8.1 never states what happens to leftover eligible members below a full batch at a grouping cutoff — user confirmed they roll over and wait for a future cutoff to complete the batch, never forming an undersized group; `GenerateDrawGroups` forms as many full groups as the backlog allows per run (not capped at one) so an accumulated backlog clears in a single execution. **6 further pre-coding architecture decisions, all resolved from the existing text rather than guessed** (`DOMAIN_LOGIC.md` §21): ordering eligible members by `members.id` instead of string-sorting Customer IDs (avoids a real "GWL10" < "GWL2" bug); once-ever grouping (a member who has ever appeared in any `draw_group_members` row is permanently excluded from future batches, matching the chit-fund-style "one batch, run to completion" reading of §8.2's "complete... snapshot" wording); unassigned dummies excluded / assigned dummies included per §14's own already-resolved rule, not a new one; no live re-check of a member's `active` status at execution time (the group's snapshot is frozen, per §8.2, and §8 unlike §6.1 writes no "skip if inactive" rule); `ExecuteMonthlyDraw` requires a pre-configured month prize and skips (never invents a value) when Super Admin hasn't set one yet; `draw_executions` rows are created directly as `executed` (the schema's NOT NULL `winner_member_id` already forces this), leaving `scheduled`/`reconciled` reserved for a future admin-correction workflow outside T-010's scope. **Scope boundary carried forward from T-006–T-009's precedent:** this task builds the grouping/execution/upline-benefit backend and the `DrawResultPublished` broadcast Event only — the actual Draw Page and its slot-machine animation are Member/Admin-facing UI already claimed by T-015/T-017 in `ARCHITECTURE.md`, and installing a real broadcast driver (Reverb, recommended) is an infrastructure/deployment decision explicitly deferred until hosting is chosen, not a T-010 blocker.
- [x] **T-011 — Implement Income Booster (qualification + 6-month schedule, concurrent multi-level)** · Completed: 13-09-2026 · Verified: `php artisan test` — 118/118 passing (Pest — 5 new tests reproducing `Docs/TEST.md` scenario 3's exact worked setup: reaching Level 1 (10 directs, 500 team split 250L/250R) creates one qualification and exactly 6 monthly schedules of ₹5,000; a 490-Left/10-Right team (500 total, split unmet) does not qualify despite meeting the combined total — the split is enforced independently; a member can hold concurrent Level 1 and Level 2 schedules once separately qualified for each, and re-evaluating while already qualified for a level never creates a duplicate row for it; `ProcessBoosterPayouts` credits due schedules, marks them paid, is idempotent on a retried run, and records 2 separate wallet ledger entries (₹5,000 + ₹20,000 = ₹25,000) for 2 concurrently-active levels in the same month rather than merging them; a later drop in direct count never affects an already-scheduled payout, since qualification is a one-time gate); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — Booster's member-facing page is T-015's job). New: `Models/{BoosterQualification,BoosterPayoutSchedule}`; `Services/BinaryTeamSizeCounter` (a single recursive SQL CTE per ancestor for Team-size's live Left/Right recompute, per §9's own implementation note); `Actions/Compensation/EvaluateBoosterQualification` (registration-payment-only trigger, evaluates every newly-qualified level per ancestor, not only the highest); `Listeners/EvaluateBoosterQualificationOnPaymentConfirmed` (the third auto-discovered listener on `PaymentConfirmed`, alongside T-006's/T-007's); `Jobs/ProcessBoosterPayouts` (logic only — scheduler wiring is T-019's job, same precedent as T-005/T-007). One new migration adds a `(member_id, level_no)` unique constraint to `booster_qualifications` (a real schema gap — only `booster_payout_schedules` had a uniqueness guard before, `DOMAIN_LOGIC.md` §21). **One genuine business-rule gap resolved via direct user confirmation before writing any code** (`DOMAIN_LOGIC.md` §21): §9's table lists a "Team size" total alongside a separate "Team split (Left–Right)" column, but never states whether the split is independently enforced or just illustrative — user confirmed each side must independently reach its own half of the threshold. **5 further pre-coding architecture decisions, all resolved from the existing text or this project's own stated principles rather than guessed:** the §31.3 Financial Integrity artifact was re-checked per the standing note from T-007/T-008 and confirmed still just a docx mistake, no new conflict; only `registration` payments trigger evaluation, never `emi_installment` (Booster's inputs are structural, unlike Level Income/Pair Reward); `BinaryTeamSizeCounter`'s live-recompute design (already specified in §9's own implementation note) was built as a single recursive CTE rather than per-node PHP recursion for performance at Level 3's 3,000-member scale; month-1 payout scheduling reuses EMI's already-established activation-date-anniversary cadence rather than inventing a new one; and `BinaryPlacementResolver::ancestorsWithSide()` is called with an explicit 10,000-level depth override (past its 500-level default trip-wire) since occupied-side traversal can legitimately produce very deep chains for a team this large — a real scalability gap surfaced by testing against the true 750/1,500/3,000-member thresholds rather than scaled-down test fixtures.
- [x] **T-012 — Implement Profile pending-fields (one-time) + Change Request workflow** · Completed: 13-09-2026 · Verified: `php artisan test` — 127/127 passing (Pest — 9 new tests reproducing `Docs/TEST.md` scenario 13's exact worked setup: submitting Pending Fields sets all 4 `members` columns and creates exactly one unverified `member_bank_details` row; a second submission attempt is rejected — fields lock after the first; invalid PAN (`INVALID123`) or Aadhaar (`12345`) format is rejected before anything is saved, no partial writes; an unverified bank detail is rejected by `SubmitPayoutRequest`, but succeeds once `VerifyMemberBankDetail` sets `verified_at`; a Change Request snapshots the live old value and leaves the field untouched until reviewed; a second pending request for the same field is rejected; approving updates the field, marks the request `approved`, and creates a database notification; rejecting leaves the field unchanged, records a `rejection_reason`, and still notifies; an approved `bank_details` change updates the same `member_bank_details` row in place — not a second row — and resets `verified_at` to null); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (2 real fixes: a `json_encode()`-can-return-`false` type gap in 2 places, handled explicitly rather than cast away); `npm run build` clean (no UI surface this task — the Complete Profile/Change Request pages are M03/M04, T-015's job). New: `Models/ProfileChangeRequest`; `Notifications/ProfileChangeRequestReviewed` (database channel — the first notification this project sends; `Member` gained the `Notifiable` trait); `Actions/Profile/{SubmitPendingProfileFields,VerifyMemberBankDetail,SubmitProfileChangeRequest,ApproveProfileChangeRequest,RejectProfileChangeRequest}`. Two new migrations: drop `members.passbook_or_cheque_path` (a genuine duplicate of `member_bank_details.proof_document_path`, discovered during the pre-coding pass) and create the standard Laravel `notifications` table (didn't exist anywhere in the project before this task). **6 pre-coding architecture decisions, all resolved from already-built schema/this project's own stated principles rather than guessed, none required asking the user** (`DOMAIN_LOGIC.md` §21): §13's "Bank Account Details" and "Passbook/Cancelled Cheque" pending fields fold into the member's one `member_bank_details` row (T-009) instead of separate `members` columns, since `members` never had bank-field columns to begin with and having the same proof-document concept live in two places risked drift; `VerifyMemberBankDetail` was built here rather than left dangling, since T-009's `SubmitPayoutRequest` already required `verified_at` but nothing ever set it; an approved bank-details change resets `verified_at` to null (a changed beneficiary account must be re-verified before its next payout use, per §11.2 point 8's own reasoning); a member may have only one `pending` change request per field at a time; PAN/Aadhaar format validation uses the standard government-mandated patterns (an objective format standard, not an invented business rule); and the notification is built now via the database channel only, since this is the first task that actually needs one — no dedicated Notifications page, matching the established UI-deferral precedent from T-006–T-011.
- [x] **T-013 — Implement Daily Dummy Entry engine + leader assignment** · Completed: 13-09-2026 · Verified: `php artisan test` — 135/135 passing (Pest — 6 new tests in `tests/Feature/DummyEntries/DummyEntryTest.php` reproducing `Docs/TEST.md` scenario 14's exact worked setup — generation creates nothing while disabled; enabling with a daily count of 3 creates exactly 3 dummy entries chained one after another down the root's Right side via real occupied-side traversal, each with a real Customer ID, `dummy_status=unassigned`, `sponsor_id`=root; a second generation run continues the same chain rather than restarting; assigning a leader activates the same row/id/Customer ID/tree position, creates their first `users` row, and records `dummy_assigned_at`/`dummy_assigned_by`; an already-assigned dummy cannot be assigned again; generation after an assignment automatically continues beneath the now-assigned leader with no special-cased tracking — **plus 2 new regression tests added to already-shipped test files** confirming the dummy-exclusion fix below: `PairRewardTest.php`'s "an unassigned dummy ancestor is skipped as a beneficiary but still lets a real ancestor further up receive theirs" and `BoosterTest.php`'s "an unassigned dummy ancestor never becomes a booster beneficiary even if it would otherwise qualify"); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — the Assignment Pool/Status view is Super Admin page territory, T-016/T-017's job). New: `Actions/DummyEntries/{GenerateDailyDummyEntries,AssignDummyEntryToLeader}`, `Jobs/RunDailyDummyEntryGeneration` (logic-only wrapper — scheduler wiring is T-019's job, same precedent as T-005/T-007/T-011), `database/seeders/CompanyRootMemberSeeder`. New migration adds `members.is_company_root`/`placeholder_name` columns; 2 new `rule_values` keys (`dummy_entry_enabled=false`, `dummy_entry_daily_count=0`, both inert defaults). **1 genuine business-rule gap resolved via direct user confirmation before writing any code** (`DOMAIN_LOGIC.md` §21): §14.1's "always right" placement mode never states an anchor point — user confirmed a single fixed seeded "Company" root Member, reused every day via the same `BinaryPlacementResolver::resolve()` occupied-side traversal real registration already uses, which naturally continues beneath whichever node is currently the chain's bottom (dummy or already-assigned leader) with no extra bookkeeping. **A real financial-integrity gap found and fixed in already-completed T-007/T-011 code, not a new business-rule question** — full detail in the `DOMAIN_LOGIC.md` §21 T-013 pre-coding pass entry and `ARCHITECTURE.md`'s new Compensation Engine rule 5: neither `CreatePairEntries` nor `EvaluateBoosterQualification` had ever excluded an unassigned dummy (or the now-seeded company root) from becoming a paid compensation beneficiary despite §14.2 point 5's unambiguous rule against it — both now skip such an ancestor while still correctly counting it structurally for whichever real ancestor sits further up the same chain. **4 further pre-coding decisions, all resolved from the existing text/this project's own established precedents:** the root also serves as every dummy's `sponsor_id` anchor, unifying §14's "Remain the company's Direct relationship" with the placement-anchor reading in one entity; dummy entries get a real sequential Customer ID at generation (not deferred to assignment), while the root itself gets a reserved out-of-sequence `GWL-ROOT` id so it never consumes a real slot; `dummy_status` transitions `generated`→`unassigned` in two steps, matching the enum's own shape and §14.3's audit-history requirement; and `AssignDummyEntryToLeader` requires no plan/payment, scoped strictly to §14.3's literal "enters real details" text — flagged as a lower-stakes open item in `DOMAIN_LOGIC.md` §21's "Still open" table if the client later confirms a plan/payment step is actually required.
- [x] **T-014 — Implement Store Management, Store Wallet, Purchase/Repurchase, Invoice, Store Profit Distribution** · Completed: 13-09-2026 · Verified: `php artisan test` — 146/146 passing (Pest — 11 new tests in `tests/Feature/Store/StoreModuleTest.php` reproducing `Docs/TEST.md` scenarios 15/16/17 and re-verifying scenarios 4/5's worked numbers on the actual store-sale path: `CreateStore` opens a wallet and credits the initial advance; a Store Wallet top-up credits correctly and a deduction beyond balance is blocked (`ValidationException`, mirroring the Member Wallet's insufficient-balance pattern); a confirmed sale decrements tracked inventory atomically and a sale exceeding current stock is blocked with zero side effects; a store-initiated sale paid from the Store Wallet deducts atomically with the sale; an Item Buyback prices strictly on _today's_ market rate (not the original sale price), restocks inventory, and creates zero `store_profit_distributions`/`income_ledger_calculations` rows; Purchase/Repurchase Income pays the purchasing member 2% "self" plus the full 12-level Sponsor/Direct chain (₹10,000 sale → ₹700 total, matching scenario 4's exact percentages) and is a no-op for a walk-in sale with no member; Store Profit Distribution pays Owner 2% + Sponsor/Direct Levels 1-3 on every store-attributed sale including a walk-in one (₹50,000 sale → ₹1,500 total, exactly scenario 5's worked numbers) while creating zero `income_ledger_calculations` rows for that walk-in case; a member purchase fires both distributions together on the same sale via the real `ConfirmStoreSale` → `StoreSaleConfirmed` event wiring, with no manual glue; `RecordPlanJewelleryDelivery` marks a plan entitlement delivered through a store, fires Store Profit Distribution for it (₹30,000 Plan-C delivery → ₹900 total), and rejects a second delivery attempt on the same entitlement; both compensation Actions are idempotent on a retried call; every confirmed sale generates an invoice); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean (no UI surface this task — the Store Owner's Inventory/Repurchases/Transactions pages are T-016's job, matching the established precedent from T-006–T-013). `php artisan migrate` also verified clean against the real Postgres dev DB, including a full rollback/re-migrate of all 4 new migrations to confirm their `down()` methods. New: `Models/{Store,StoreWallet,StoreWalletLedgerEntry,StoreInventoryItem,StoreSale,Invoice,StoreProfitDistribution,StoreBuyback,StoreActivityLog}`; `Services/StoreWalletService`; `Actions/Store/{CreateStore,AllocateStoreInventoryItem,RecordStoreWalletTopup,ConfirmStoreSale,RecordItemBuyback,RecordPlanJewelleryDelivery}`; `Actions/Compensation/{CalculatePurchaseRepurchaseIncome,CalculateStoreProfitDistribution}`; `Events/StoreSaleConfirmed` + 2 auto-discovered listeners. 4 new migrations: `store_inventory_items`, `store_buybacks`, `store_sales`/`product_benefits` gain the inventory-link/delivery columns, and `wallet_ledger_entries.category` gains `purchase_repurchase_income`. 4 new `rule_values` keys (`purchase_repurchase_income_rates`, `store_profit_distribution_rates`, `item_buyback_percent=60`, `store_gst_percent=0`). **Did the required pre-coding pass before any code** (`Docs/DOMAIN_LOGIC.md` §21's new T-014 log, §16.8–§16.10): 3 architecture decisions resolved directly from already-confirmed text, none needing to ask the user — a sale needs a stock/quantity check mirroring the Store Wallet balance-check pattern (§16.8); an Item Buyback structurally cannot trigger Store Profit Distribution or Purchase/Repurchase Income, since both rules only ever describe a _sale_ and a Buyback is the reverse direction (§16.9); §16.4 scenario 3's "new joining with jewellery delivery at a store" is wired via an explicit, optional `RecordPlanJewelleryDelivery` action rather than an automatic side effect of registration, since delivery is conditional on actually happening through a store (§16.10). One genuine spec gap flagged rather than guessed, added to `DOMAIN_LOGIC.md` §21's "Still open" table: the store sale GST/tax percentage's real statutory value is never stated anywhere in the source documents — seeded as a Super-Admin-configurable setting defaulting to 0 (inert), the same "default 0 until confirmed" pattern already used for the Withdrawal Processing Fee. **A real cross-database migration bug found and fixed while verifying against the real Postgres dev DB, not just SQLite** (this project's established practice of running `php artisan migrate` against Postgres, not only trusting the SQLite test suite): Laravel's `enum(...)->change()` generates invalid Postgres syntax for a column that already has a named `CHECK` constraint (an inline `CHECK` inside an `ALTER COLUMN TYPE` clause, which Postgres's grammar rejects) — fixed by dropping and recreating the named constraint directly via raw SQL on `pgsql`, keeping the normal Schema-builder path on `sqlite` where this conflict doesn't arise; recorded in `Docs/DATABASE_SCHEMA.md`'s migration table as a note for any future enum-column change.
- [x] **T-019 — Wire scheduled jobs (draw grouping/execution, dummy generator, booster payout, EMI due processor, report export worker) with idempotency and retry-safety** · Completed: 13-09-2026 · Verified: `php artisan test` — 153/153 passing (Pest — 7 new tests in `tests/Feature/Jobs/ScheduledJobsTest.php`: exactly the 6 recurring jobs are registered, none more; every job runs with an explicit `Asia/Kolkata` timezone and `withoutOverlapping()`; each job's cron expression matches its documented §19 timing — daily at 00:30/00:05/00:45 for the Dummy Generator/EMI Due Processor/Booster Payout Processor respectively, `monthlyOn(15, '00:00'/'12:00')` for the Draw Group Generator/Monthly Draw Executor, and the Pair/Reward Monthly Evaluator resolving the actual last day of the current month rather than a hardcoded "30"; the Report Export Worker is confirmed absent from the schedule since it's "On demand" and its Job class doesn't exist yet); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build` clean; manually verified via `php artisan schedule:list -v` against the real app showing all 6 jobs with correct cron expressions and `+05:30` next-run times. New: `App\Console\Scheduling::register()` (the actual `Schedule::job(...)` registrations, called from `bootstrap/app.php`'s new `withSchedule` closure), `Jobs/{RunDrawGroupGeneration,RunMonthlyDrawExecution}` (T-010's `GenerateDrawGroups`/`ExecuteMonthlyDraw` Actions had no Job wrapper yet, unlike T-005/T-007/T-011/T-013's jobs). **4 architecture decisions resolved directly, none needing to ask the user, recorded in `DOMAIN_LOGIC.md` §21/§19** (the source spec left several jobs' exact time-of-day unspecified — the _day_ was always fully determined by existing rules, only the _time_ was open): Daily Company Direct Generator at 00:30 Asia/Kolkata (source spec: "exact time TBD"); EMI Due Processor at 00:05 (source: "Daily"); Pair/Reward Monthly Evaluator at 23:30 on the last calendar day (source: "Month-end"); Booster Payout Processor **daily** at 00:45, not monthly (source: "Scheduled" only) — since each `booster_payout_schedules` row's own due date follows the member's individual activation-date anniversary, a monthly-on-one-fixed-day check would miss members whose due date falls on a different day of the month. **A real, non-obvious Laravel testing quirk found and worked around, not a business-rule issue:** `bootstrap/app.php`'s `withSchedule()` wires its callback through a `Illuminate\Console\Application::$bootstrappers` static array gated on `Artisan::starting`, which fires unpredictably (and can double-register events) in a single test process that runs multiple Artisan invocations (RefreshDatabase's own internal `migrate` call, plus the test's own commands) — this has no bearing on real production behavior (a fresh PHP process per `schedule:run` cron tick fires it exactly once) but made the wiring untestable via direct `Schedule::class` container resolution. Fixed by extracting the actual job registrations into `App\Console\Scheduling::register()`, callable directly against a plain `new Schedule()` in tests, sidestepping the Artisan lifecycle entirely — `bootstrap/app.php` itself is now a 1-line pass-through. Also documented (`Docs/ARCHITECTURE.md`, not a code change): production deployment must use a real per-minute OS cron entry running `php artisan schedule:run`, never the `schedule:work` convenience command, since `lastDayOfMonth()` recomputes its target day fresh only when the whole app reboots each tick. **Scope note carried forward:** the Report Export Worker mentioned in this task's own title has no Job class yet since T-018 (which defines what a report export actually is) hasn't been built — nothing was wired for it; §19 itself already lists it as "On demand", not a recurring cadence, so nothing will need to be _scheduled_ for it later either, only dispatched on request once T-018 exists.
- [x] **T-015 — Build Member Portal pages M01–M18** · Completed: 14-09-2026 · Verified: `php artisan test` — 163/163 passing (Pest — 10 new tests in `tests/Feature/MemberPortal/MemberPortalTest.php`: a guest is redirected to login for any member portal route; a member sees the real M01 dashboard while a Super Admin still sees the untouched generic placeholder at the same shared `/dashboard` route; Pending Profile fields submit exactly once with real file uploads (asserted against a faked `public` disk), a second attempt is rejected, and an invalid PAN format is rejected before anything is saved; a Change Request is blocked before Pending Profile fields are submitted and succeeds after, snapshotting the old value and leaving the live field untouched until reviewed; a Payout request is blocked with no verified bank details and succeeds once verified, correctly placing a wallet hold; the EMI page's Pair/Reward eligibility indicator shows the correct required-vs-completed-installment counts for a real EMI plan member; a member can view their own Wallet/Level Income/Pair-Reward/Booster/Draw/Payment-History/Notifications/Membership/Reports pages; a member can download their own Payment History as CSV); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (a real, project-wide Larastan/Carbon date-cast gap surfaced across a dozen new controllers, fixed once via a new shared `App\Support\Dates::date()` helper rather than repeating the workaround inline everywhere, matching the tooling note `ARCHITECTURE.md` already carried from T-005); `npm run build`/`types:check` clean. **Full manual browser QA performed, not skipped** (all 18 M01-M18 pages, including the pre-existing M08/M09 Directs/Tree views now integrated into the new Member Portal navigation shell) — logged in as a real member via the actual `/member/login` flow against the real Postgres dev DB and clicked through every single page, confirming correct data, no console errors, and correct active-nav-item highlighting in the new grouped sidebar (Overview/Profile/Membership & Payments/My Network/Income/Draw & Wallet/More). New: `Http/Controllers/DashboardController` (top-level — the shared `/dashboard` route branches on `$request->user()->member`, keeping the untouched generic starter-kit dashboard for non-member roles until T-016/T-017 build their own) and 14 controllers under `Http/Controllers/Member/` (`Profile`, `PendingProfile`, `ChangeRequest`, `Membership`, `PaymentHistory`, `LevelIncome`, `PairReward`, `Booster`, `Draw`, `Wallet`, `Payout`, `Reports`, `Notification`), each thin — validating via a Form Request then calling straight through to the Actions T-006–T-014 already built, never re-implementing any business logic; `Http/Requests/Profile/{SubmitPendingProfileFieldsRequest,SubmitProfileChangeRequestRequest}`, `Http/Requests/Payout/SubmitPayoutRequestRequest`; `App\Support\Dates` (see the Larastan note above); `resources/js/layouts/member-layout.tsx` + `resources/js/components/member-sidebar.tsx` (replacing the generic `AppLayout` for every `member/*` page, including M08/M09 which had been sitting in it since T-004 with no real Member Portal navigation around them); 16 new `resources/js/pages/member/*.tsx` pages plus `emi.tsx` extended in place (paid date/reference/mode columns + the Pair/Reward eligibility indicator, both explicitly deferred at T-005). Also extended `Member` (2 new relations: `incomeLedgerCalculations`, `walletLedgerEntries`, `pairRewardTransactions`) and `ProductBenefit` (`store()` relation) models — pure read-access additions, no schema/migration change. **A real, significant infrastructure bug found during the manual QA pass (not by the automated suite, which always seeds fresh) and fixed at the root, not worked around** — full detail in `Docs/DOMAIN_LOGIC.md` §21's new T-015 log: `RuleVersionSeeder`'s guard checked "does any active rule version exist" rather than "does this specific key exist for the active version," so on the real dev database (seeded once during T-003, never fully reset since) every key T-006 through T-014 added afterward — `booster_levels`, `pair_milestones`, `payout_min_amount`, `draw_group_size`, `purchase_repurchase_income_rates`, `store_gst_percent`, and 11 others — was silently never written; every affected feature had been running against `RuleVersionService`'s inert `$default` fallback in the real dev app the whole time (invisible to Pest, which always migrates+seeds fresh). Fixed by making the seeder idempotent per-key (`firstOrCreate` on `(rule_version_id, key)` instead of a blanket per-version guard) and re-running it once against the real dev database to backfill all 16 missing keys — purely additive, no existing member/registration data touched. **Scope note carried forward:** M17 (Reports) here is a lightweight "download my own data as CSV" page only — the full cross-role, cross-module report catalog with queued exports (`Docs/INSTRUCTIONS.md`'s "Reports" section) is T-018's own dedicated task.
- [x] **T-016 — Build Admin/Store Owner pages A01–A06** · Completed: 14-09-2026 · Verified: `php artisan test` — 174/174 passing (Pest — 10 new tests in `tests/Feature/AdminPortal/AdminPortalTest.php`: a member is forbidden from every admin portal route; an admin with no assigned store is forbidden from every admin portal route; an admin with a store sees the real A01 Store Dashboard at the shared `/dashboard` route with correct store name/wallet balance; an admin can update only `contact`/`location` on their store profile; a new sale generates an invoice and a `store_sale_new_sale` activity log entry; a sale against tracked inventory decrements stock and deducts the Store Wallet correctly; an Item Buyback resolves a member by Customer ID and prices on today's market rate; a Plan Jewellery Delivery marks the product benefit delivered; Inventory/Store Transactions/Store Reports pages return 200; a store's own sales CSV downloads correctly); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors (3 more `nullsafe.neverNull` occurrences of the same `$x?->prop ?? $default` pattern first hit in T-015, fixed the same way — an explicit `$var = $x !== null ? $x->prop : $default;` statement); `npm run build`/`types:check` clean. **Full manual browser QA performed, not skipped** — logged in as a real Admin/Store Owner (a freshly created test account owning a real store with real inventory, created via `php artisan tinker`) against the real Postgres dev DB via `php artisan serve`, clicked through and exercised all 6 A01-A06 pages including submitting a real sale through the browser form and confirming the invoice/activity-log/recent-transactions update; test data and the dev server were cleaned up afterward. New: `Http/Controllers/Admin/{StoreDashboard,StoreProfile,Sales,Inventory,StoreTransactions,StoreReports}Controller`; `DashboardController` gained an admin-with-store branch delegating to `StoreDashboardController` (mirroring M01's member branch); `Http/Middleware/EnsureStoreOwnership` (alias `store-owner`) resolves the one `Store` a `role=admin` user owns, 403s if none; `Http/Requests/Admin/{RecordStoreSaleRequest,RecordItemBuybackRequest,RecordPlanJewelleryDeliveryRequest,UpdateStoreProfileRequest}`; `resources/js/layouts/admin-layout.tsx` + `resources/js/components/admin-sidebar.tsx` (mirroring the Member Portal's shell) + 6 new `resources/js/pages/admin/*.tsx` pages. **Two real bugs found and fixed during the required manual QA pass, neither a business-rule question — full detail in `Docs/DOMAIN_LOGIC.md` §21's new T-016 log:** (1) a genuine, previously-unimplemented DOMAIN_LOGIC.md §16.3 requirement — no T-014 Store Action was writing `store_activity_logs` rows at all — fixed with a new `App\Services\StoreActivityLogger` called from every Store Action, plus a new regression test; (2) a Card-list item-row visual overlap (a two-line left text block crowded against a bare-text right value with no `gap` and `items-center` vertical-centering) on `admin/inventory.tsx`'s two rows plus 2 pre-existing T-015 pages (`member/level-income.tsx`, `member/wallet.tsx`) that shared the exact same pattern — fixed uniformly with `items-start`/`gap-3`/`shrink-0 whitespace-nowrap`. **A third, much larger gap surfaced by the same QA pass and fixed with the user's explicit go-ahead to sweep the whole app in this same checkpoint, not deferred:** the Indian DD-MM-YYYY date format (a hard "must" requirement since T-003, recorded in AGENTS.md) had never actually been applied to any dynamic date anywhere in the running app — `App\Support\Dates::date()` (used by every T-015/T-016 controller) only ever produced plain ISO `YYYY-MM-DD`, and no frontend date-formatting helper existed at all. Fixed with one new shared `formatDate()` helper in `resources/js/lib/utils.ts`, applied at every dynamic-date display site across all 13 affected Member Portal and Admin Portal page files; re-verified visually in the real browser and via a clean `npm run types:check` before rebuilding. No backend or database change was needed — DD-MM-YYYY is applied purely at display time, per the project's standing "format at display time, not storage" rule.
- [x] **T-017 — Build Super Admin pages S01–S10 + Admin Dashboard/Member/Compensation/Draw management** · Completed: 14-09-2026 · Verified: `php artisan test` — 195/195 passing (Pest — 21 new tests across `tests/Feature/SuperAdmin/SuperAdminSettingsTest.php` (8 tests directly exercising the 5 new Actions: rule-version publishing carries every prior key forward and overlays only the changed ones, a published version is immediately visible — cache correctly busted, not stale for up to 60s; Admin user creation sets role=admin with a hashed password; Store Owner reassignment updates ownership and logs an activity entry; a metal rate entry creates a new effective-dated row without touching prior rates; draw reconciliation sets status/metadata without touching the winner; a correction note creates a separate audit record rather than editing the execution; an already-reconciled draw cannot be reconciled again) and `SuperAdminPortalTest.php` (13 tests: role-gating on every route, and a happy-path test per write endpoint)); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean. **Full manual browser QA performed on all 16 screens, not skipped, including several real writes** — logged in as the real seeded Super Admin against the real Postgres dev DB and exercised: a compensation rule-version publish (S03), an Admin user creation (S02), a metal rate entry (S07), a dummy-entry-leader assignment (S05), and a full draw reconciliation with a correction note (S06/Draw Management) — all confirmed end-to-end with real data appearing correctly formatted (DD-MM-YYYY throughout) on return visits. New: 13 controllers under `Http/Controllers/SuperAdmin/` (`DashboardController` S01, `AdminUserController` S02, `RuleVersionController` S03, `DummyEntrySettingsController` S04, `DummyEntryAssignmentController` S05, `DrawSettingsController`+`DrawManagementController` S06, `MetalRateController` S07, `PayoutTdsSettingsController` S08, `StoreManagementController` S09, `StoreWalletManagementController` S10, `MemberManagementController` + `CompensationManagementController` for Admin Member/Compensation Management), joining the existing `CashPaymentApprovalController` (T-003) in the same namespace; 12 Form Requests under `Http/Requests/SuperAdmin/`; the top-level `DashboardController` gained a `role === 'super_admin'` branch delegating to `SuperAdmin\DashboardController` (mirroring M01/A01's precedent — the generic starter-kit placeholder is now unreachable for every real role); `Member` gained a `storeProfitDistributions()` relation; new `resources/js/layouts/super-admin-layout.tsx` + `super-admin-sidebar.tsx` (6 grouped nav sections, 13 items) + 16 new `resources/js/pages/super-admin/*.tsx` pages. **6 new backend Actions, since almost none of Super Admin's write-paths existed before this task (unlike T-015/T-016, which mostly wrapped Actions already built):** `Actions/Settings/PublishRuleVersion` (one shared writer behind S03/S04/S06/S08/Admin-Compensation-config's versioned settings, rather than 5 near-duplicate Actions), `Actions/Settings/SetMetalRate` (S07), `Actions/Admin/CreateAdminUser` (S02), `Actions/Store/ReassignStoreOwner` (S09), `Actions/Draw/ReconcileDrawExecution` (S06's reconciliation half — 2 new `draw_executions` migration columns, `reconciled_at`/`reconciled_by`, plus a new `draw_execution_corrections` audit table and `DrawExecutionCorrection` model). Migrations run clean against the real Postgres dev DB. **Full pre-coding pass done before any code, per this project's standing discipline** — 5 architecture decisions resolved directly from already-established `DOMAIN_LOGIC.md` text, none needing to ask the user (full detail in `DOMAIN_LOGIC.md` §21's T-017 pre-coding entry): the shared `PublishRuleVersion` design; Admin-user-creation and store-owner-assignment as two separate steps (T-014's `CreateStore` never created the owner account itself); S06's reconciliation resolving to a minimal status-close-out design (never touching the immutable draw result fields) rather than an unspecified re-pick/void mechanic; Member Detail's "audit/activity history" as a synthesized read-side feed from existing tables, not a new audit-log table; the new `storeProfitDistributions()` relation. **One genuine spec gap flagged rather than guessed, added to `DOMAIN_LOGIC.md`'s "Still open" table:** `IncomeLedgerCalculation`'s missing reversal/reference column for the Compensation Audit page — no rule anywhere describes reversing a finalized compensation calculation, consistent with this project's "no refunds, ever" principle, so very likely inert by design; not built. **3 real, small issues found and fixed during manual QA, none a business-rule question — full detail in `DOMAIN_LOGIC.md` §21's T-017 build & QA entry:** a Member Detail "Recent Activity" feed sort-order bug (was sorting already-formatted date *strings* alphabetically instead of the underlying timestamp — fixed by sorting before formatting, not after); S04's dummy-entry "Generated" count double-counting the seeded company-root Member (excluded explicitly); a raw 36-character UUID reference shown as the first, most prominent text in the Store Wallet ledger row (fixed by moving it after the description and truncating to 8 characters). **This task's QA pass was itself interrupted mid-way by the session hitting its usage limit** — per the user's explicit instruction, progress was checkpointed in `Docs/PROGRESS.md` before/at the interruption so the resumed session needed no rework: it re-verified the already-passing automated suite, then continued QA exactly where it left off (confirmed S01/S02/S03/S09-index were already done, picked up from S09's detail page) rather than re-testing anything already confirmed.
- [x] **T-018 — Build Reports module (all report types + queued exports)** · Completed: 14-09-2026 · Verified: `php artisan test` — 203/203 passing (Pest — 8 new tests in `tests/Feature/SuperAdmin/ReportExportTest.php` reproducing `Docs/TEST.md` scenario 18: a queued export transitions `pending`→`processing`→`ready` with the correct filtered `row_count`, and the generated CSV — parsed back — contains exactly the matching rows with a comma-containing field correctly round-tripping through proper `fputcsv()` escaping; a zero-matching-rows report still completes `ready` with `row_count=0` and a header-only file, never left `pending`; a mid-generation exception marks the row `failed` with `error_message` populated, never left stuck `processing`; every `super-admin/reports/*` route 403s for `member`/`admin` roles, including a direct `download` attempt on another user's ready export by ID; spot-check correctness tests for `paymentOut`'s recipient-via-payout-request-member mapping and `storeDistribution`'s beneficiary-level mapping against real seeded data); `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean. **Full manual browser QA performed, not skipped, with real queue processing** — logged in as the real seeded Super Admin against the real Postgres dev DB, requested exports across all 3 formats (a Payment In PDF, a Store Sales XLSX, a Membership CSV), ran `php artisan queue:work --stop-when-empty` against the real `QUEUE_CONNECTION=database` queue (exactly as a production worker would), confirmed each completed `ready` with the correct row count and downloadable, correct file content — independently re-verified in a second pass (a fresh Wallet/Ledger CSV request, processed and its file content read directly off disk) before this task was signed off. New: `report_exports` migration + `App\Models\ReportExport`; `App\Services\ReportCatalog` (11 methods, one per `INSTRUCTIONS.md` "Reports" table row, each a `cursor()`-backed query against the already-existing models — `Member`, `EmiInstallment`, `IncomeLedgerCalculation`, `PairRewardTransaction`, `DrawExecution`, `BoosterPayoutSchedule`, `Payment`, `PayoutTransaction`, `WalletLedgerEntry`, `StoreSale`, `StoreProfitDistribution` — no new report-data tables, only the tracking one); `Actions/Reports/RequestReportExport`; `Jobs/ProcessReportExport implements ShouldQueue` (writes csv via `fputcsv()`, xlsx via a new `App\Exports\ReportExportSheet` + `maatwebsite/excel`, pdf via a new `resources/views/reports/export-pdf.blade.php` + `barryvdh/laravel-dompdf` — 2 new composer packages); `App\Support\Dates::display()` (DD-MM-YYYY formatting for dates written directly into a server-generated file, where the frontend `formatDate()` pass never runs); `SuperAdmin\ReportsController` (index/store/download) + `Http/Requests/SuperAdmin/RequestReportExportRequest`, routes added to the existing `routes/super-admin.php`; new `resources/js/pages/super-admin/reports.tsx` + a new "Reports" `super-admin-sidebar.tsx` section. **Full pre-coding pass done before any code** (`Docs/DOMAIN_LOGIC.md` §21's new T-018 entry, `Docs/TEST.md` scenario 18): T-018 is a new, separate, Super-Admin-only Reports Center that does not touch M17 (Member's own small/synchronous reports, T-015) or A06 (Admin's own store-scoped reports, T-016) — per §2's "no company-wide access" rule for Admin, 9 of the 11 report types are structurally Super-Admin-only data to begin with; every export is queued uniformly rather than building row-count threshold-detection logic; filters kept deliberately generic (date range + optional customer_id/store_id) rather than bespoke per report type; CSV uses proper `fputcsv()` escaping from the start (M17/A06's older naive `implode(',', ...)` pattern is left alone since neither page's columns actually contain a comma-risk field today, but T-018's free-text member/store name columns are exactly the case that pattern would silently corrupt). **One genuine spec gap resolved without inventing a number:** Wallet/Ledger's "balance" column has no backing running-balance data anywhere in the schema (only `Member.wallet_balance`, the member's *current* balance, un-attributable to a specific historical row without replaying the whole ledger) — omitted the column from that report rather than fabricate a number, showing the member's current balance alongside each row instead, documented inline. **A genuinely queued job needs a real, continuously-running queue worker in production** (`php artisan queue:work`, typically under Supervisor) — `QUEUE_CONNECTION=database` was already configured but unused until this task; installing/configuring that persistent worker process is a deployment decision, explicitly deferred the same way T-010 deferred choosing a real broadcast driver, not a T-018 blocker, since the Job itself is code-complete and was verified correct against the real queue table end-to-end.
- [x] **T-020 — Add automated test coverage per risk-based coverage table** · Completed: 15-09-2026 · Verified: `php artisan test` — 206/206 passing (default suite, SQLite); `php artisan test --configuration=phpunit.concurrency.xml` — 3/3 passing against the real dev Postgres database, confirmed non-flaky across 3 repeated runs; `vendor/bin/pint --test` clean; `vendor/bin/phpstan analyse` (level 7) — 0 errors; `npm run build`/`types:check` clean; zero leftover throwaway fixtures confirmed in the real dev database after every concurrency run. User explicitly asked this task focus on concurrency, since an audit of the existing 203-test suite found solid *sequential* idempotency coverage everywhere but zero genuine parallel/race-condition test anywhere. **New `tests/Concurrency/` suite (3 tests) proves real row-level-locking safety under actual simultaneous OS processes** (`Illuminate\Support\Facades\Process::start()`, never in-process simulation — PHP is single-threaded, so nothing short of real separate processes produces a genuine race), against the real dev Postgres database (SQLite `:memory:` can't even be shared across 2 connections, and its whole-database locking would validate the wrong thing anyway) via a new, separate `phpunit.concurrency.xml` config excluded from the default `php artisan test` run: (1) `StoreConcurrencyTest.php` — 3 simultaneous ₹400 Store Wallet deductions against a ₹1000 balance, confirmed exactly 2 succeed/1 blocks and the final balance is exactly ₹200, no lost update, no overdraft; (2) `StoreInventoryConcurrencyTest.php` — 3 simultaneous sales of quantity=2 against 5 units of stock, confirmed exactly 2 succeed/1 blocks and final stock is exactly 1; (3) `BoosterPayoutConcurrencyTest.php` — 3 simultaneous scheduler-tick-equivalent processes against one due, `pending` booster payout schedule, confirmed it's paid exactly once (not zero, not twice) with exactly one `wallet_ledger_entries` row and the exact correct amount. Each test is backed by a tiny, single-purpose new `app/Console/Commands/Concurrency*.php` worker command (not user-facing functionality) launched as the real child process. **A real schema gap was found and fixed along the way — not a concurrency-safety bug** (the actual `lockForUpdate()` crediting logic proved correct): `booster_payout_schedules.wallet_ledger_entry_id` had no `ON DELETE` action, so deleting a Member with a paid booster schedule could hit a foreign-key violation from two independent cascade paths off the same `members` row racing in Postgres's own internal FK-action ordering; fixed with a new migration (`ON DELETE SET NULL`), verified via both `migrate` and `migrate:rollback`. **Also individually audited the risk-based coverage table's remaining 6 non-concurrency rows against the existing suite** (not assumed covered by proximity) — found and filled 2 real, narrow gaps: Level Income never actually exercised a Sponsor-vs-Binary-Position *divergence* scenario (only asserted in docblocks, not tested) or a genuine 3rd-decimal-place rounding case; Binary placement's occupied-side traversal test only covered the Left side, not the Right side the table explicitly asks for. Pair/Reward consumption, Payment webhook idempotency, and Payout processing were confirmed already thoroughly covered — no padding added. Purchase/Repurchase Income's duplicate-beneficiary rule was confirmed a structural (unconstructable-input) guarantee already covered by `SponsorChainResolver`'s own design, not independently testable — no test added, per the standing "don't test what can't happen" discipline. **Full pre-coding pass done before any code** (`Docs/DOMAIN_LOGIC.md` §21's new T-020 entries, `Docs/TEST.md`'s new "Concurrency Verification" section, which also corrects scenario 15's regression note — it previously referenced a Store Wallet concurrent-deduction test that never actually existed, aspirational text written ahead of the test being built and never followed up): payment-webhook double-delivery was considered and deliberately excluded from the concurrency scope, since its idempotency is already enforced by a DB unique constraint rather than a `lockForUpdate()` critical section, so it fails safely under a real race regardless of timing — flagged as a small, separable follow-on if the client's real transaction volume ever makes it worth the added test-infrastructure cost.
- [x] **T-021 — Resolve open client-confirmation items before their dependent feature ships** · Completed: 13-09-2026 · Verified: documentation-only (no code) — the client answered the last remaining tracked open item (store inventory/repurchase/returns/POS details, previously "explicitly deferred to the client") directly in chat: (1) inventory tracked item-wise (name/metal/weight/quantity/price/description); (2) repurchase has no quantity/weight limit; (3) no returns/refunds exist anywhere for jewellery, but a genuinely new concept the client introduced — **Item Buyback**, the store buying back a previously-sold item at a Super Admin-configurable % (default 60%, user-confirmed based on _current_ market rate, not original sale price) — fills the gap the "returns" question was really pointing at; (4) store-sale payment collection happens entirely outside the application. Written into `Docs/DOMAIN_LOGIC.md` §16.5 (new), §16.6 (new), §16.7 (new), §17.3 (rewritten from PENDING to RESOLVED), §17.4 (new), and a new §21 resolved-log entry. **T-014 (Store module) and everything depending on it (T-015/T-016/T-017/T-018) is now unblocked** — no schema/code was written this pass, per the standing "resolved query ≠ permission to start coding" instruction; T-014's own pre-coding pass (schema design, ARCHITECTURE.md notes, TEST.md worked scenarios) still needs to happen before any implementation code, whenever the user gives go-ahead to start it.
